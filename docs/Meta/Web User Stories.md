---
title: Web User Stories
description: User stories for the web UI's chat and kiln-editing flows, with acceptance criteria and test-tier mapping
tags: [meta, ux, web, user-stories, testing]
updated: 2026-09-13
---

# Web User Stories

Stories for the two web surfaces that matter now: **chat** and **kiln/note editing**. Test tiers:

| Tier | Mechanism | Determinism |
|------|-----------|-------------|
| **W1 unit** | vitest + @solidjs/testing-library (jsdom, mocked fetch) | full |
| **W2 ui** | Playwright against `bun run dev`, `page.route()` + mock SSE frames (`just web-test ui`) | full |
| **W3 visual** | Playwright `toHaveScreenshot()` baselines at key states; video+trace ON for story specs | full (pixel-tolerant) |
| **W4 e2e-live** | Playwright against the `cru` and the bundle this tree just built, a real daemon, a temp kiln and a fake model server (`just web-test live`) | deterministic model, real stack |

**W2 is `ui`, not `e2e`.** It mocks every API route in the browser, so it proves
what a component does with an answer the spec itself wrote. That is worth
having and it is not end to end. The name said otherwise for a year, and four
defects on the session path shipped through a green run of it.

**W4 is the only end-to-end tier, and it is strict about what "the product"
means.** The setup runs the `cru` this tree built and serves the `dist` this
tree built, and fails the run when either is absent or older than its sources —
no fallback to a `cru` on PATH. It is hermetic: own HOME, own XDG roots, own
`CRUCIBLE_HOME` and config dir, provider credentials and proxies dropped, and a
fake Ollama server as the only model the daemon can reach.
`e2e/live/lane-guard.live.spec.ts` asserts each of those from inside the run.

The live tier's session coverage: `session-path.live.spec.ts` (a draft becomes a
real session, its first turn is answered, and the files panel lists the
session's own folder; every kiln the chip offers attaches and detaches; a
session read after a daemon restart carries its modes and its model; the kiln
switches mid-session), `session-management.live.spec.ts` and
`session-lifecycle.live.spec.ts` (both converted from mocked specs that asserted
only that a request fired).

W2 specs double as **image sequences**: `screenshot()` after each scripted step into the test's artifact dir; W3 pins the key frames; video records the whole story.

## Coverage governance

The tiers only help if scenarios move between them deliberately. These rules decide when.

**Promote a GAP / manual scenario to an automated tier when all three hold:**
- **Deterministic** — the outcome is fixed given the inputs. Mock the model at the wire (fake-ollama / mock-acp-agent) so replies are scripted; wait on conditions (`expect.poll`, `toBeVisible`), never `page.waitForTimeout`.
- **Acceptance-criteria-shaped** — the story has a concrete "then" you can assert via a role/label/text/testid locator, not a raw CSS class and not an eyeball.
- **Broke once** — a real regression slipped through here. Promotion buys the most where it already cost us.

Until a GAP meets all three, leave it marked GAP with a one-line note on what blocks automation.

**Graduate a ui-tier scenario (W2) to the live tier (W4) when the assertion depends on anything the daemon decides** — real session persistence, resume from history, kiln-file bytes on disk, whether a call is admitted at all, or cross-console visibility with the TUI. W2's `page.route()` mocks answer 200 because the fixture says 200, so a refusal is invisible there by construction. If a story's "then" is "the daemon accepted it", "the same state is visible on disk" or "the other console sees it", it belongs in W4.

**Every new feature adds a story and a tier before it merges.** A behavior with no WS entry and no tier is untested by definition. Add the story (with acceptance criteria), pick the lowest tier that can prove it, and — if it crosses the daemon boundary — add the W4 leg too.

---

## 1. Chat

### WS-101: Send a message and watch it stream
**As a user**, I type a message, hit send, and watch the reply stream in token-by-token with a working indicator.
**Acceptance:** input clears on send; deltas append without re-render jumps; completion shows token usage; SSE reconnects transparently on drop.
**Tests:** W2 (exists), W3 (streaming mid-state + complete baselines — `chat-stream.story.spec.ts`). W4 live streaming is intentionally NOT covered: the web session route hardcodes the internal agent, so `mock-acp-agent` is unreachable from the browser and there is no deterministic in-tree provider; the streaming path is covered deterministically at W3.

### WS-102: Thinking blocks
**As a user**, extended thinking renders as a collapsible block distinct from the answer.
**Acceptance:** collapsed by default with token count; expanding reveals content; hidden from copy/export of the answer.
**Tests:** W1 (exists), W2 story.

### WS-103: Tool calls as cards
**As a user**, tool executions render as cards with name, status spinner → result, and expandable output.
**Acceptance:** parallel calls render as separate cards keyed by call_id; errors styled distinctly; large output truncated with expansion.
**Tests:** W1 (ToolCard pinned 90%+), W2 (exists), W3 baseline.

### WS-104: Approve or deny a permission from the browser
**As a user**, when the agent requests permission mid-turn I get a card on the prompt with tool, args, and a diff preview; my choice (allow once/session/project, deny) resumes the turn.
**Acceptance:** the card opens from `interaction_requested` SSE and DOCKS above the prompt — square bottom corners on the card, square top corners on the field, no gap between them — so the answer sits where the user types. The transcript keeps a one-line record of the open request ("Asked to use update_note · waiting"), which names the file a write asked for and quotes the question an ask asked. The card's type sits at the transcript sizes (11px mono for the argument listing, 12px for the rest). A diff renders old/new for file writes; the choice POSTs `/api/interaction/respond`; deny yields an error tool card and the turn continues; queued requests open sequentially. A request the daemon still holds survives a reload: the stream carries only NEW requests, so `ChatContext` reads the pending aggregate once when it binds to a session, and a reloaded page shows the card instead of answering every send with 422.
**Tests:** W1 (`ChatInput.test.tsx` — `draws the full card directly above the prompt`, `squares the prompt`, `draws no card while nothing is pending`; `MessageList.empty-state.test.tsx` `MessageList interaction record` — the write record, the ask record, and nothing while no request is open; `contexts/__tests__/ChatContext.pending-restore.test.tsx` — the aggregate seeds the card on bind, and a request for another session is ignored). W2 full flow (`permission.story.spec.ts` — allow-once/scope/deny payloads + write-diff + queued sequence). W4 live is excluded for the same reason as WS-101 (mock-acp-agent unreachable from the web session route).

### WS-105: Answer agent questions (Ask)
**As a user**, single-select, multi-select, and free-text questions render as modals I can answer or cancel.
**Acceptance:** all Ask variants render; cancel sends a cancelled response; answer resumes the turn.
**Tests:** W1 (exists), W2 (`ask.story.spec.ts` — single/multi-select + free-text). GAP documented: `AskInteraction` has no cancel affordance, so "cancel sends a cancelled response" is unimplemented.

### WS-106: Switch model mid-conversation
**As a user**, a model picker below the input lets me switch models without losing history.
**Acceptance:** picker lists `/api/session/:id/models`; switch POSTs and is reflected in subsequent turns; history intact.
**Tests:** W2 (exists — verify post-switch turn), W3.

### WS-107: Sessions: create, switch, resume, auto-title
**As a user**, I create sessions, switch between them, resume old ones with full history, and see auto-generated titles I can override.
**Acceptance:** the daemon auto-titles a session on its first completed turn and broadcasts `title_changed`; the web renders the pushed title everywhere (tab, session list, inbox) and never generates titles client-side (never overwrites manual titles); switching loads `/history` correctly; end/archive states visible.
**Tests:** W2 (partial; `title-generation.spec.ts` — `title_changed` SSE renames tab+list, untitled fallback label, no client calls to the title endpoints — realigned 2026-07-12 after the daemon took ownership of titling), W4 (resume against real persistence — GAP).

### WS-108: Cancel a turn
**As a user**, a stop control cancels the in-flight turn, preserving partial output.
**Acceptance:** cancel POSTs `/api/session/:id/cancel`; stream closes cleanly; partial message retained with a cancelled marker.
**Tests:** W2 (`cancel.story.spec.ts` — stop control → `/cancel` POST → `[cancelled]` marker).

### WS-109: Export a session
**As a user**, I export the conversation as markdown from the browser.
**Acceptance:** export dialog offers formats; downloaded content matches `/api/session/:id/export`.
**Tests:** W1 (exists), W2 download flow (`export.story.spec.ts` — preview + downloaded bytes match `/export`).

### WS-110: One gutter for every turn, and a prompt that is the size of its text
**As a user**, each turn carries its actions in one column at the right of the transcript, and my own prompt is as wide as the words in it — with the time it was sent only under the pointer, and never at the cost of a pixel of layout.
**Acceptance:** every transcript row is content plus a fixed right-hand column (`TurnGutter`, `--cru-turn-gutter`), and the column is the same one on a prompt, on an assistant turn and on a system row. The column holds Copy and Edit for a prompt, Copy and Regenerate for a turn, and the turn's elapsed time (`8.9 s`, the clock time for a turn replayed from history) to the LEFT of those buttons. The column appears on hover or on keyboard focus, and stays on where the device reports no hover; its pointer events follow its opacity, so an invisible button never takes a click. The actions no longer hang under the turn, so the `pb-5` band and the `mb-6` margin that reserved room for them are gone: the ONLY vertical space between two rows is `--cru-turn-gap`, spent once by `MessageList` (and by `ChatContent`'s loading skeleton, so nothing shifts when the history lands). The column is taken OUT of `--cru-measure-chat` rather than added to it, so the reading text keeps the composer's left and right edges. A prompt bubble (`.user-quote`) is `width: fit-content` with a `max-width` clamp, so a three-word prompt draws a three-word box; the sent time is an INLINE trailer at the end of the prompt's last line, always in the document, and only its opacity answers the hover. The box therefore measures the same with and without the pointer — the reserved room costs the tail of one line and no height at all — and the time wraps with the last line instead of taking a line of its own.
**Tests:** W1 (`Message.test.tsx` — `is hidden at rest and revealed on hover or keyboard focus`, `changes NOTHING but opacity between rest and hover`, `sits inside the prompt paragraph, so the last line reflows around it`, `declares fit-content with a clamp, not a full-width block` (read out of `index.css`), `holds the actions in a column beside the prompt, never under it`, `keeps the column on a system row so the reading edge does not move`; `AssistantTurn.test.tsx` — `holds the actions in the same column every turn kind uses`, `is revealed on hover or focus, and is always on where there is no hover`, `reserves NO vertical room for a footer`, `shows how long the turn took, LEFT of the buttons`; `MessageList.grouping.test.tsx` — `spaces the rows from --cru-turn-gap, and no row adds a margin of its own`).
**Live-verified 2026-09-15:** in a real session at 1200x900, a user bubble measures 99.89 x 35.58 px with the pointer off it and 99.89 x 35.58 px with the pointer on it — the same box — while the stamp goes from opacity 0 to 1. The assistant gutter reads `5.1 s`, Copy, Regenerate, left to right. A prompt wrapped over two lines keeps its stamp on the second line. A pane narrowed to 260 px keeps the 84 px column and never scrolls sideways, which leaves 140 px of reading width: the column is a fixed cost, and a very narrow pane pays it.

## 2. Kiln & Note Editing

### WS-201: Browse workspace files and kiln notes
**As a user**, the file tree shows a real hierarchical folder tree for the selected root; clicking opens the file. (Superseded/expanded by **WS-217** — the unified explorer. The Files panel is no longer a standalone left tab: it is embedded in the **Navigator** panel, which absorbed Files/Sessions/Search.)
**Acceptance:** kiln roots build the whole tree from `list_notes` (folders split from kiln-relative paths); project roots lazy-load one level via `fs.list_dir`; folders expand/collapse with type icons; click opens via `openFileInEditor`.
**Tests:** W1 SHIPPED 2026-07-18 — the tree *UI* GAP is now covered: `FileTreeView.test.tsx` (real zag machine — `role=tree`, `aria-level`, folders-first order, click→open-leaf-exactly-once routing-seam guard, branch-click-does-not-open, `aria-current="page"`), `kiln-builder`/`collection`/`reconcile`/`tree-root`/`treeRootStore` suites. **Remaining GAP:** W2 Playwright (context-menu interactive open, drag) and W4 live (`fs.list_dir` project walk against a real daemon) still deferred; the daemon `fs.list_dir` security properties are Rust-unit-covered (`server::fs::tests`).

### WS-202: Edit a note and save it
**As a user**, I open a note in the CodeMirror editor, type, and see a dirty ● on the tab. The note saves itself 2 s after my last edit, or at once on Cmd-S or the Save button, and the dot clears. A file outside every kiln — project code, config — saves only when I ask.
**Acceptance:** a save is a whole write through `PUT /api/kiln/file`, and it carries the base the buffer was read from. A clean save clears dirty and moves the buffer's base to the hash the daemon answered. A save carries the base TEXT beside the base hash, so a note someone changed meanwhile is merged rather than refused: a clean merge writes, and the buffer takes what the route wrote and goes clean. A merge that leaves a region writes nothing — the buffer stays dirty, the writing waits in the outbox as a conflict, and the conflict view opens on it (WS-322). A save whose buffer holds no base text is refused, and nothing is written. A save the daemon never answered is queued in the outbox and the buffer goes clean; when that write lands from the outbox while the note is open, the buffer takes the answered hash, so its next save is not refused for the user's own write. A failure the daemon answered with (a 403 on a read-only project) keeps dirty + shows a toast with a retry. Content round-trips exactly (frontmatter, unicode, wikilinks untouched). Autosave (decision log 2026-09-11) arms only for a file inside a kiln; a stored `autosaveSeconds: 0` from before settings version 2 migrates once to 2, and a 0 chosen after it is kept.
**Tests:** W1 the write door (`lib/offline/__tests__/sync.test.ts` — `sends the base it was edited from when the daemon answers`, `refuses a stale save online and queues nothing`, `queues the writing when the daemon never answered`, `raises a refusal the daemon answered with, and queues nothing`), the buffer (`contexts/__tests__/EditorContext.test.tsx` — `a buffer opened from a read carries its text as the base text`, `a stale desktop save with a clean merge lands and moves the base`, `a stale desktop save with regions opens the conflict view and keeps the buffer dirty`, `marks the buffer clean and takes the answered hash on a clean save`, `a queued save marks the buffer clean and takes no base`, `a drained write moves the open buffer's base when the bases match`); W1 autosave (`FileViewerPanel.test.tsx` — a note autosaves after the interval, a project file never does, 0 stays off) and the migration (`lib/settings.test.ts`); W2 full round-trip via the real editor harness (`editor-roundtrip.story.spec.ts` — open → dirty ● → PUT body → clean; save-failure keeps dirty), **W2 through the SHIPPED app** (`editor-shipped-app.story.spec.ts` — real `App` mount, file opened via the product `openFileInEditor` path, real `FileViewerPanel`, Save button + Cmd-S), W4 save-lands-on-disk (`kiln-truth.live.spec.ts` — byte-exact temp-kiln file).
**Product bugs — FIXED (were bugs 3/4/8):** (3) `App.tsx` now mounts `<EditorProvider>` around `WindowManager` (+ a `crucible:open-file` event to open a path in the editor programmatically), so the editor is reachable in the shipped app; (4) `FileViewerPanel` now has a Save button + Cmd/Ctrl-S wired to `EditorContext.saveFile`; (8) the editor now loads AND saves by absolute path through `/api/kiln/file` (`getFileContent`/`saveFileContent`) — the old `getNote()` returned metadata only (`get_note_by_name` has no `content`), and `saveNote()`'s note-name route broke for notes in subdirectories (URL-encoded slash). `GET`/`PUT /api/kiln/file` were also fixed server-side to accept absolute paths (containment is still enforced by `find_enclosing_kiln` + within-kiln canonicalization); previously the absolute-path ban made those routes reject every real editor request.
**Latent loop fixed alongside:** with the editor finally reachable, `FileViewerPanel`'s dirty-sync effect read `windowStore` (via `findTabByFilePath`) and wrote it (via `updateTab`) in one tracked scope → self-retriggering stack overflow; the write is now `untrack`ed, and `Pane.renderContent` re-renders only on tab identity/type change (not on `updateTab` ref churn) so edits are not discarded.

### WS-203: Multi-file tabs with unsaved-changes safety
**As a user**, multiple open files show as tabs; each tracks its own dirty state; closing a dirty tab warns me.
**Acceptance:** switching tabs preserves per-file undo/content; dirty markers independent; close-with-unsaved prompts (or documents that it discards).
**Tests:** W2 via the real editor harness (`editor-tabs.story.spec.ts` — content preserved across tab switches, clean tabs stay clean across open/switch, dirty-close confirm dismiss/accept, clean-close never prompts) + W1 (`CodeMirrorEditor.test.tsx` sync-vs-edit, `EditorContext.test.tsx` close guard, `tab-guards.test.ts`).
**Product bugs — FIXED 2026-07-12 (were bugs 5/6):** (5) programmatic doc swaps into the reused CodeMirror instance are now tagged with a `contentSync` annotation and ignored by the update listener, so only real user edits mark a file dirty. Both panels' duplicated inline editors were consolidated into one shared `components/editor/CodeMirrorEditor.tsx` (which also initializes the view from the ref callback — vitest's solid pipeline fires `onMount` before refs, so an onMount-based init never mounts under jsdom). (6) `closeFile()` now confirms before discarding a dirty file (`{force}` opt for callers whose close was already confirmed), and window-tab close paths (`TabBar`, `WindowManager` closeActiveTab) route through `confirmTabClose()`, which checks the tab's `isModified` mirror.

### WS-204: Syntax-aware editing
**As a user**, markdown, rust, and js/ts files highlight appropriately in the editor.
**Acceptance:** language detected by extension; theme consistent (one-dark); large files stay responsive.
**Tests:** W1 (`CodeMirrorEditor.test.tsx` — per-extension language table), W3 markdown highlight baseline (`editor-roundtrip.story.spec.ts`).
**Product gap — FIXED 2026-07-12 (was bug 7):** `getLanguageExtension()` (now in the shared `components/editor/CodeMirrorEditor.tsx`) maps md/markdown, js/jsx/mjs/cjs, ts/tsx, and rs onto the installed CodeMirror language packages; unknown extensions fall back to plain text.

### WS-205: Writes are kiln-safe
**As a user/operator**, the browser can never write outside the kiln or workspace roots.
**Acceptance:** traversal names (`../`, absolute) rejected by `PUT /api/notes/:name` and `PUT /api/kiln/file` with 4xx; oversized content rejected (MAX_CONTENT_SIZE).
**Tests:** Rust route tests (exist), W4 live (`kiln-truth.live.spec.ts` — traversal note name rejected 4xx by the real daemon; nothing written outside the kiln).

### WS-206: Chat and editor share one kiln truth
**As a user**, a note the agent creates in chat appears in the file tree, and a note I edit is what the agent reads next turn.
**Acceptance:** tree refreshes on note creation events; agent tool reads reflect saved edits (through daemon, no cache staleness).
**Tests:** W4 (the flagship live-stack story — `kiln-truth.live.spec.ts`: a note written through the browser is the one shared truth — appears in the kiln tree and is byte-exact on disk, what an agent tool reads next turn).

### WS-207: Backlinks panel for the focused note
**As a user**, with a note focused in the editor, a Backlinks panel shows which notes link here (linked mentions) and which notes this note mentions without linking (unlinked mentions), each unlinked mention convertible to a real wikilink in one click.
**Acceptance:** panel fetches `GET /api/backlinks?kiln=&note=` for the focused note's kiln-relative key; linked mentions list source title + path and open the source on click (`crucible:open-file`); unlinked mentions come from the daemon's `suggest_links` over the note's content, with self-mentions filtered server-side; clicking **Link** rewrites the *open editor buffer* (`[[target]]` or `[[target|mention]]`), marks it dirty, and removes the suggestion; non-markdown/absent focus shows an empty state. A FAILED fetch is not an empty state: a transport or server error draws the shared `EmptyState` in its error tone, names the HTTP status, and offers **Retry**. The old catch rendered "Linked mentions (0) / No notes link here yet." for a 404, which told the user a lie about their kiln.
**Tests:** W1 (`BacklinksPanel.test.tsx` — sections, note-key derivation, empty states, the error tone with its status and Retry, open-event, one-click insertion; `note-actions.test.ts` — `insertWikilink` offset/drift/double-wrap cases), W2+W3 (`backlinks-panel.story.spec.ts` — real panel beside the real editor via the harness `?backlinks=1`; visual baselines `backlinks-panel.png` + `backlinks-after-link.png`; asserts the buffer rewrite, dirty ●, suggestion removal, and linked-mention open), Rust route contract tests (`route_contract_tests/kilns.rs::backlinks_*` — param validation, 404, traversal rejection, self-mention filtering, degraded unlinked on missing file). Daemon side: `get_backlinks` RPC over the `note_links` reverse index with candidate matching (stem / path / extension-less path / title, case-insensitive, fragment-stripped) — unit-tested in `kiln_manager.rs`.
**Scope note:** "unlinked mentions" are *in this note* (what `suggest_links` computes). Obsidian-style *incoming* unlinked mentions (other notes mentioning this title) would need a kiln-wide text scan — deferred.

### WS-208: Wikilink hover previews everywhere
**As a user**, hovering any `[[wikilink]]` — in a chat message, in the editor, or on a backlinks row — floats a preview card with the note's title, path, and a rendered excerpt; clicking the card title opens the note.
**Acceptance:** one document-level controller (`WikilinkHoverPreview`, mounted once in `App`) reacts to any element carrying `data-note`; preview resolves via `getNote` + `/api/kiln/file` with frontmatter stripped and the excerpt truncated on a line boundary; misses render "Note not found"; results cached per kiln+name; card dismisses on hover-away and survives hovering the card itself; aliased links (`[[note|alias]]`) display the alias but resolve the target.
**Tests:** W1 (`WikilinkHoverPreview.test.tsx` — show/dismiss/missing/click-through/data-kiln; `note-actions.test.ts` — excerpt, caching, degradation; `markdown.test.ts` — alias + fragment target parsing), W2+W3 chat surface (`wikilink-hover.story.spec.ts` — anchor renders in a streamed assistant turn, preview shows rendered markdown, dismisses, click opens an editor tab; baseline `chat-wikilink-hover-preview.png`), W2+W3 editor surface (`wikilink-navigation.story.spec.ts` — baseline `editor-wikilink-hover-preview.png`).

### WS-209: Follow wikilinks in the editor
**As a user**, `[[wikilinks]]` in a markdown buffer are visibly link-styled; Ctrl/Cmd+Click or **Mod-Enter** (cursor inside the link) opens the target note; a plain click just moves the cursor.
**Acceptance:** CodeMirror `MatchDecorator` marks links (`.cm-wikilink` + `data-note`, alias/fragment-normalized) in markdown files only; Ctrl/Cmd+mousedown and a `Prec.high` Mod-Enter keymap (so `insertBlankLine` from defaultKeymap doesn't shadow it — and still runs outside links) call the panel's follow handler; following is navigation, not an edit (buffer stays clean); missing targets warn via toast.
**Tests:** W1 (`wikilink-extension.test.ts` — decorations incl. incremental edits, alias/fragment targets, target-at-cursor, both gestures, plain-click inertness), W2+W3 (`wikilink-navigation.story.spec.ts` — decorated baseline `editor-wikilink-decorated.png`, hover preview, Ctrl+Click opens a tab, Mod-Enter follows with the source staying clean).

### WS-210: Editor feel — vim keybindings, markdown preview, shell theming
**As a user**, the editor feels like mine: vim modal editing by default (toggleable in Settings), an Edit ↔ Preview toggle that renders the note like Obsidian's reading view, and chrome that matches the ember shell instead of oneDark's blue-grey.
**Acceptance:** vim (`@replit/codemirror-vim`) is ON by default via the persisted `editor.vimMode` setting (Settings → Editor toggle, applies live to open editors); the buffer opens in normal mode (`x` deletes, `i` inserts); the preview toggle (corner button / Mod-Shift-E — plain Ctrl-E belongs to vim scroll) renders through the same markdown pipeline as chat, with frontmatter stripped and wikilinks as live `data-note` anchors (hover cards + click-to-open); switching files returns to edit mode; non-markdown files get no toggle; editor background/gutters use shell tokens (`crucibleEditorChrome` must precede oneDark — earlier CM6 extensions win); a wikilink reads the same in the transcript and in a note — ember ink on a faint wash, no underline at rest, an underline on hover — and one rule set states it for BOTH themes, because the light theme used to underline a wikilink and a heading that the dark theme left plain. A link title drops its extension: `[[Getting Started.md]]` is one link reading "Getting Started", not the text "Getting" beside a link to "Started.md" that `linkify` produced while it read `.md` and `.ai` as country domains.
**Tests:** W1 (`EditorWithPreview.test.tsx` — toggle render/back, frontmatter strip, wikilink click-through, non-md exclusion, per-file reset, vim x-deletes vs no-vim inertness; `components/__tests__/link-treatment.test.ts` — one rule set, no underline at rest, underline on hover, never on a heading, and every rule stated once for both themes; `lib/__tests__/markdown.test.ts` — the extension dropped from the title and the TLD list left at its stock default), W2+W3 (`editor-vim-preview.story.spec.ts` — vim normal/insert mode e2e with the product default, preview baseline `editor-markdown-preview.png`; re-pinned editor baselines carry the shell chrome + pill styling). Story harness runs vim-off by default (`{ vim: true }` opts into the product default) since most stories type plain text.
**Scope note:** superseded in part by WS-212 — the reading view remains the third mode; inline live preview shipped.

### WS-211: Panel chrome that holds still — 1px separators, fixed toggles, draggable collapsed tabs
**As a user**, the workspace chrome behaves like Obsidian's: separators between panels are hairlines (not fat bars), the collapse/expand controls stay put when a panel changes state, and a collapsed panel's tabs are still first-class — I can drag them anywhere.
**Acceptance:** edge-panel and split separators render as 1px lines with an invisible ±4px grab zone (`after:` pseudo), highlighting on hover and ember on drag; the expanded panel content draws no border of its own (the separator line is the border, and it faces the center on every side — the right panel's border sits on its left); the collapsed strip anchors its expand button in a fixed top `h-9` slot (mirroring the tab-bar row it replaces) on vertical panels and pinned to the right end on the bottom strip; the expanded edge tab bar keeps an in-place collapse button in the same corner; collapsed strip icons are draggable with the standard tab payload (drag out to any pane/bar; drop onto a strip lands in that panel and expands it). **Swap Side Panels moves the panes, and every field travels with them** — collapse included, so a collapsed tree stays collapsed on the side it lands on. The earlier rule held collapse on the SIDE, so one flip revealed the stowed tree; the owner chose the pane rule. A swap also stops REMOUNTING: the shell row and every split render as keyed lists, so a flip is a reorder, and a transcript, a terminal and a scroll position all survive it.
**Tests:** W1 (`stores/__tests__/swapSidePanels.test.ts` — each side's panes move, the flip is its own inverse, and the focus ring follows the panes; `windowing/__tests__/swap-remount.test.tsx` — the same DOM node and the same component instance across a flip, a centre pane carried across, and the rail chrome on the side the rail landed on; `resize-handles.test.ts` — 1px + grab-zone + fixed-slot contract; `dnd-remount.test.tsx` — draggables re-register with the live group id after a layout restore swaps ids, for both collapsed strips and edge bars — the silent-no-op bug that made collapsed-pane DnD dead in the shipped app), W2 (`tab-reorder.spec.ts`, `cross-zone-dnd.spec.ts`, `windowing-comprehensive.spec.ts` cover drags across the new chrome).
**Extension (unified drag abstraction, 2026-07-16):** any surface can join the drag system by carrying a Tab payload — `DragSource` gained a group-less `'newTab'` variant placed by `placeNewTab` (pane split, tab bar, edge panel with expand-on-drop, floating window at a drop point via `resolveNewTabTarget`; duplicate identities focus instead of duplicating). W1: `tab-placement.test.ts`. Superseded as the hover-card mechanism by WS-213 (hover popovers ARE floating windows), but remains the extension point for future drag sources (file-tree rows, search results).

### WS-213: Hover popovers are windows; panels grow out of ribbons (Obsidian parity)
**As a user**, hovering a wikilink opens a real floating editor window (Obsidian Hover Editor), and every edge panel grows out of an always-visible icon bar (Obsidian ribbon) — one window abstraction, no bespoke popups, no chrome that vanishes.
**Acceptance:** hover spawns a TRANSIENT `FloatingWindow` (same component as pop-outs/tear-offs) next to the anchor with a real editor inside; it auto-closes on hover-away unless pinned (titlebar pin, or auto-pin on drag/resize — Hover Editor semantics); transient windows and their tab groups never persist to the saved layout; DOM churn under a stationary pointer (loading overlays, CodeMirror redecoration) must NOT dismiss it (note-name + anchor-rect tracking); only loading/not-found render a small card; popovers open in the configured `editor.hoverMode` — default the fully rendered reading view. Floating titlebar (slim, 1px border): pin (transient only) | title=drag | tab-bar toggle (compact popovers hide the tab bar; showing it exposes native tab DnD) | dock | roll-up | maximize-with-restore-bounds (leaves the far-left ribbon visible) | close. Edge ribbons render unconditionally at all three edges; ribbon icons toggle their panel (expand+activate / collapse-on-active-click / switch), the panel appearing between ribbon and center; the flyout subsystem is REMOVED (ribbon click = expand). Every ribbon leads with its panel's expand/collapse toggle; the left ribbon hosts command buttons (palette modal, new session; settings gear pinned at the bottom, Obsidian's own layout). Floating windows AUTO-CLOSE when their last tab is removed or dragged out. FileViewerPanel's loading overlay only covers files that have no content yet (EditorContext.isLoading is global).
**Tests:** W1 (`WikilinkHoverPreview.test.tsx` — spawn/dismiss/pin/dedupe/missing-card; `floatingWindow.transient.test.ts` — pin, persistence exclusion, maximize restore-bounds; `resize-handles.test.ts` — ribbon contract), W2+W3 (`wikilink-hover.story.spec.ts` — popover open/dismiss/pin in chat, baseline `chat-wikilink-hover-preview.png`; `wikilink-navigation.story.spec.ts` — popover in the editor harness, baseline `editor-wikilink-hover-preview.png`; windowing e2e realigned to ribbon semantics).

### WS-212: Live preview — prose-first markdown editing (Obsidian-style)
**As a user**, markdown notes open in live preview: styled prose — sized headings, bold/italic/strikethrough, inline code as a mono chip, wikilinks as pills showing their display text — with the syntax characters hidden; the ONE construct my cursor is in shows its raw source for editing, and ONLY inline code/code blocks render monospace. The raw mono flow is a mode toggle (source), and the fully rendered reading view stays on Mod-Shift-E.
**Acceptance:** live preview is the DEFAULT for `.md`/`.markdown` (never active for other files, which stay source with no mode controls); a ViewPlugin over the lezer syntax tree hides `#`/`**`/`*`/`~~`/backticks/link+wikilink brackets and styles content, revealing marks when a selection touches the construct (whole line for headings); aliased wikilinks display only the alias; bare `[text]` (a URL-less lezer Link node) is NOT treated as a link; fenced code and blockquotes get line styling; the content font is prose (Plex Sans) with mono reserved for code; typing inside a revealed construct keeps it raw (edit-in-place); markdown parses with the GFM base (strikethrough, tables, task lists); switching files returns to the default mode; live preview matches the reading view's chrome — NO line-number gutter (source mode keeps it) and a configurable readable line width (`editor.maxLineWidth`, default 768px, 0 = full) shared with the rendered view; markdown TABLES render as real HTML tables (sanitized through the chat pipeline) until the selection enters the table — clicking a rendered table drops the cursor in for raw editing (block widgets via StateField; CM6 forbids block decorations from ViewPlugins).
**Tests:** W1 (`live-preview.test.ts` — hide/style per construct, cursor-reveal exclusivity, heading line-reveal, aliased wikilinks, edit-in-place, no-extension source behavior; `EditorWithPreview.test.tsx` — live default, mode toggle round-trip, non-md exclusion), W2+W3 (`editor-live-preview.story.spec.ts` — baselines `editor-live-preview.png` (styled) and `editor-live-reveal.png` (cursor inside inline code, everything else styled); `wikilink-navigation.story.spec.ts` + `backlinks-panel.story.spec.ts` realigned to pill display with cursor-reveal).

### WS-214: Saving without a save bar
**As a user**, buffers save the way I configure — not via a toolbar bolted onto every editor: Mod-S or Mod-Enter from the keyboard, an idle autosave interval, and an optional status-bar affordance showing the active buffer's dirty state.
**Acceptance:** FileViewerPanel has no save toolbar; Mod-Enter saves except when the cursor is ON a wikilink (follow wins; insertBlankLine is deliberately shadowed); `editor.autosaveSeconds` (Settings → Editor, 0 = off) saves a dirty buffer after that many idle seconds, each keystroke resetting the countdown; `editor.showSaveButton` toggles a status-bar dirty-dot + Save for the active file that disappears once clean.
**Tests:** W1 (`FileViewerPanel.save.test.tsx` — no toolbar, autosave fires/holds at 0; `EditorWithPreview.test.tsx` — Mod-Enter saves off-link), W2 (`editor-shipped-app.story.spec.ts` — dirty surfaces in the status bar, status-save PUTs and clears, Mod-S path; `hero.live.spec.ts` realigned).

### WS-215: One design system — tokens, one collapse control, motion
**As a user**, the shell looks like one product: every surface, border, text tint, hover, and status color comes from the ember design tokens; each panel has exactly one collapse control; structural surfaces animate in instead of popping.
**Acceptance:** all colors route through the `@theme` tokens in `index.css` (shell-bg/panel, surface-elevated/overlay, control, shell-ink/body, muted/muted-dark, hairline/hairline-strong, hover-wash, error/ok/attention/precog; sky-* exempt as the running-agent hue until a blue token exists); no raw Tailwind gray/status palette classes and no white-alpha borders in `src/`; the ribbon toggle is the only in-panel collapse control (the header keeps Obsidian's corner toggles); floating windows/hover popovers pop in, edge panels slide out of their ribbon, palette/modal fade+pop, drop indicators fade — mount-only, `prefers-reduced-motion` respected, keyframes animate `scale`/`translate` (never `transform`, which would clobber Tailwind's centering transforms); markdown buffers parse a leading `---` block as YAML frontmatter, kept raw mono in live preview; live preview wraps prose lines.
**The token contract and the consistency rules (2026-09-15, picks R1–R6):** every colour, radius, row height and type size is a `--cru-*` custom property in `@layer cru-theme`, and the `@theme` block holds aliases only, so a plugin's unlayered `:root` rule wins in both themes with no `!important` — see [[Help/Extending/Web Theme Tokens]]. No component carries a raw px, hex or rgba outside a named allow-list, and every token is declared in both themes. One `EmptyState` component draws every empty pane: a title, one body line, an optional action, NO icon, and an error tone that a failed fetch uses instead of the empty one. Every interactive element that clears its outline carries the shared `focus-ring` utility, no component sets a size below the 11px floor, a badge colour carries one meaning (CMD and the permission card's tool badge are neutral), a tab title caps at 200px and elides from the MIDDLE with the full label in `title`, an icon button keeps a 32px hit area on the desktop, and the three row heights are `--cru-row-sm/md/touch`. Tailwind drops an `@import` that follows `@plugin`, silently, so a gate holds every `@import` ahead of the first other at-rule.
**Tests:** W1 (`style-consistency.test.ts` — repo-wide no-raw-palette + no-white-alpha-border sweep, motion-class presence, keyframe property contract, `every outline-none carries the focus-ring utility`, `no component sets a size below the 11px floor`, `no component hard-codes a colour, a radius, a type size or a row height`, `every @import precedes the first @plugin or @theme`; `lib/__tests__/contrast.test.ts` — the `--cru-*` contract: both theme blocks inside the layer, every alias an alias, every token resolving in both themes, and the AA ratios; `components/__tests__/EmptyState.test.tsx` — no icon, the error tone, the actions; `windowing/__tests__/TabBar.title.test.tsx` — the middle elision, the 200px cap, the full label in `title`, and no fade on a short active title; `resize-handles.test.ts` — no duplicate collapse control; `live-preview.test.ts` — frontmatter + wrapping), W2 (`editor-live-preview.story.spec.ts` — frontmatter mono band + `cm-lineWrapping`). The dead FlexLayout CSS layer (~1200 lines, incl. the unwired light theme) was deleted outright.

### WS-216: A real terminal, a clean panel roster, one background hierarchy
**As a user**, the Terminal tab is a real shell (xterm.js over a PTY), every tab in the default layout is an implemented panel, and panel chrome shares one background hierarchy.
**Acceptance:** the terminal panel runs the user's `$SHELL` in a PTY over `/api/terminal/ws` (localhost-only, same gate as `/api/shell`) with ANSI colors from the ember palette, resize sync, scrollback, and a reconnect affordance when the session ends; the placeholder panels (Explorer/Search/Source Control/Outline/Problems/Output "Coming Soon" stubs) are DELETED — components, registrations, and default-layout tabs. The default roster (updated since: the **Navigator absorbed the separate Files/Sessions/Search tabs** into one left panel with a scope swapper) is: left **Navigator** (single tab), right **Backlinks + Activity**, bottom **Terminal + Chat**; persisted layouts with the retired tabs are remapped by `migrateRetiredPanels` (`windowStoreInternals.ts`); backgrounds follow one rule — app canvas `shell-bg` (header, ribbons, tab bars, status bar), panel content `shell-panel` (editor, terminal, panels; the active tab carries `shell-panel` so it fuses with its content, Obsidian-style), `surface-elevated` for raised rows, `surface-overlay` only for popups/floating windows.
**Tests:** W2 (`terminal.story.spec.ts` — mocked PTY WebSocket: prompt renders in xterm, input echoes, drop → reconnect), W1 (windowing/store suites updated to the implemented-only roster; `panel-placeholders.spec.ts` deleted with its subject).

### WS-217: Unified file-tree explorer — pick any kiln or project root
**As a user**, a top-right dropdown lets me browse any registered **project** or **kiln** as a real hierarchical file tree, and the tree stays live as files change. (The explorer now lives inside the **Navigator** left panel — see WS-216's roster note.)
**Acceptance:** the dropdown groups roots as Projects / Kilns (from `project.list` + `kiln.list`, deduped), selection persists across reload (localStorage); selecting a kiln builds the whole tree from `list_notes`, selecting a project lazy-walks one folder level per expand via the new daemon `fs.list_dir` RPC; keyboard/ARIA tree (`role=tree`, roving focus, arrows in/out of folders, Enter=open, F2 reserved for Phase-2 rename), sort (name/modified asc/desc), collapse-all, reveal-active, read-only right-click menu (Open/Copy path/Reveal-in-tree); kiln trees patch in place on live `GET /api/fs/events` SSE (non-`.md` leaves ignored), project roots refresh-on-interaction (manual refresh + window-focus refetch of expanded folders). **Security (daemon-side):** `fs.list_dir` is allowlisted fail-closed to registered projects and to the workspace folder the daemon made for a project-less session (`<session_scratch_dir>/<id>`, matched by shape and by the session's record; a stranger folder under the base is refused — `server::fs::tests::only_a_real_sessions_folder_is_admitted_under_the_scratch_base`), rejects `rel_path` traversal before disk, canonicalize-and-contains, drops per-entry symlinks escaping the root, and hides dotfiles + gitignored entries by default (`show_ignored` reveals).
**Tests:** W1 (`FileTreeView`/`RootDropdown`/`kiln-builder`/`collection`/`reconcile`/`tree-root`/`treeRootStore` — 173 assertions incl. path-splitting, roster grouping/persistence/dedup, reconciler idempotence + `moved`==delete+create convergence), Rust (`server::fs::tests` — nested walk, gitignore/dotfile hiding, symlink-escape exclusion, traversal rejection, dirs-first sort; `web::fs_events::tests` — daemon→SSE event mapping). **GAP:** W2 Playwright interactive (context-menu open, keyboard journey) and W4 live (`fs.list_dir` + live SSE against a real daemon) deferred; project live-watching (`ProjectManager` watcher) deferred to a follow-up.
**Scope:** Phase 1 of a 3-phase plan. **Phase 2** = drag-to-move, rename, mkdir, delete-to-trash, DnD drop-lines; **Phase 3** = deterministic resolved-link index + wikilink auto-rewrite on move. Seams laid: node `status` field (future git/diff decoration), daemon-as-single-write-authority. (Phase-2 DnD SHIPPED as WS-218; the `FileDragSource` seam types were superseded by `lib/file-dnd`.)

### WS-218: Drag files — move in tree, open in a pane, insert a link in the editor
**As a user**, I can drag any file-tree row and (a) drop it on a folder (or the tree root) to MOVE it on disk, (b) drop it on an editor pane to open it there, or (c) drop it into open editor text to insert a `[[wikilink]]` (kiln md → md) or the rel path — three behaviors, one drag.
**Acceptance:** native-HTML5 drags via `@atlaskit/pragmatic-drag-and-drop` (`lib/file-dnd.ts`) so file drags cross the solid-dnd windowing provider boundary; every file drop target tags a `zone` and only the INNERMOST zone acts (editor beats pane beats tree-root). Folder targets highlight + auto-expand after 700ms hover; guards reject cross-root, into-own-parent (no-op), and dir-into-own-descendant drops. Moves go through `POST /api/fs/move` → daemon `fs.move` (same fail-closed threat model as `fs.list_dir`: registered projects or ALREADY-OPEN kilns only, parent-only canonicalize-and-contain so symlinks move as links, overwrite/root/into-self rejected); the tree reloads after a move (kiln SSE reconcile is idempotent alongside). Pane drops open via `openFileInGroup` (tab lands in THAT pane's group); editor drops insert at `posAtCoords` with `dropCursor` feedback.
**Tests:** W1 (`file-dnd.test.ts` — drop guards, target rel, insert text, innermost-zone protocol; `windowStore.ghostGroup.test.ts` — see fix below), Rust (`server::fs::tests` move suite — escapes both ends, overwrite, dir-into-self, symlink-parent escape, symlink-moves-as-link; `route_contract_tests/fs.rs` — 200/`moved`, 422 on missing fields). **W4 live-verified 2026-07-18** (real browser + daemon): tree-move relocated a file on disk, tab-strip drop opened the file in that pane, editor drop inserted `[[Researcher]]` at the pointer (and did NOT also open — innermost wins). **GAP:** no automated W2 Playwright drag journey (synthetic DragEvent dispatch works; `page.dragAndDrop` fights hover-intercepting layout chrome).
**Phase 3 SHIPPED 2026-07-18 (link integrity):** kiln `.md` moves through `fs.move` now route through the daemon's wikilink-aware rename (`note.rename`/`note.move` RPCs): a **resolved-link index** (`note_links` v2 — deterministic resolution persisted per occurrence with byte spans) drives a descending-byte-span splice of exactly the unambiguous inbound links; alias/heading/block/embed forms and the author's bare-vs-path style survive; ambiguous stems are never touched (UI shows "N links not auto-updated"). Backlinks read the same index (fuzzy matcher deleted). **Move invariant:** bare-stem links keep resolving across folder moves with zero text churn — the index re-resolves by `target_key` on every note add/remove, so it converges even for out-of-band moves (shell `mv`). Live-verified round-trip (root → `sub/deep/` → root), rename with rewrite, and ambiguous-skip against a real daemon.

**Pre-existing bugs found by the live pass — FIXED:** (1) lazily loaded project folder children were silently DISCARDED (expand → empty): ark/zag's collection is controlled, so the machine returns the merged tree via `onLoadChildrenComplete` and the owner must persist it — `FileTreeView` now hands `d.collection.rootNode` up (`onLoadedTree`) and `FilesPanel` stores it. (2) every center open (tree click, palette, drop) silently no-op'd on layouts whose pane referenced a `tabGroupId` missing from `tabGroups` (pre-v3 ghost): `addTab` now materializes the group from the pane's authoritative id instead of returning early.

### WS-219: Right-click menus — tree mutations, tab closing, editor clipboard
**As a user**, right-click gives me app menus where the browser steals keybinds — but never at the cost of native copy: Shift+right-click and images/links always show the BROWSER menu.
**Acceptance:** Tree rows: read actions + **Rename** (inline ark rename input → `fs.move`, so kiln `.md` renames rewrite inbound wikilinks via the Phase-3 pipeline), **New note** (kiln folders; toolbar `+` for root), **New folder** (`fs.mkdir`), **Delete** (`fs.trash` → `.crucible/trash/<stamp>-<name>`, excluded from indexing/watching; open tabs under the trashed path force-close). Tabs (center + edge): Close / Close Others / Close to the Right, each through the dirty-tab confirm guard. Editor: Cut/Copy/Paste/Select All/Copy File Path via `navigator.clipboard` + the live `EditorView`. **Native fall-through** (`lib/context-menu.ts`): capture-phase guard stops our triggers on Shift+right-click or targets inside `img, a[href], [data-native-menu]` — Copy Image / Save As always work. All ark `Menu.ContextTrigger`s use `asChild` divs (the default trigger is a `button`; rows/tabs/editors contain interactive content).
**Security:** `fs.mkdir` whitelists components + contains the deepest EXISTING ancestor (symlinked prefixes cannot escape); `fs.trash` refuses `.crucible/*` and never overwrites (timestamp + counter); both use the fs.move allowlist (registered projects / OPEN kilns).
**Tests:** W1 (`context-menu.test.ts` — native-fall-through rules, close-mode set math; `FileTreeView.test.tsx` — capability matrix: new-note kiln-dirs-only, no `move` entry (DnD owns it); `mutations` helpers — `.md` preservation on rename labels). Rust (`server::fs::tests` — mkdir escapes/symlink-prefix, trash no-overwrite/`.crucible` refusal/dir-children collection; contract tests for both routes). **W4 live-verified 2026-07-19**: menu rename rewrote `[[target]]`→`[[renamed]]` on disk, delete landed in `.crucible/trash/`, editor + tab menus opened with correct items, Shift+right-click did NOT open the app menu.
**GAP:** `window.prompt/confirm` for new-note/new-folder/delete names (inline-input polish later); no automated Playwright journey (same drag/hover-chrome limitation as WS-218).

### WS-220: A session opens beside its rail; the centre reads sessions | editor
**As a user**, starting or opening a session never takes over my editing surface, and I never hunt for the pane it landed in: a session opens in the centre pane NEXT TO the sessions rail, and a file opens in the centre pane next to the files rail. The Edit/Session header mode pills are gone — there is no center "mode" to toggle (goEdit/goSession remain as command-palette commands). Legacy session-less generic `chat` tabs are pruned from persisted layouts on restore (they made the active session render wherever they were docked).
**Acceptance:** `openSessionInChat` puts the session tab in the centre pane on the sessions rail's side and activates it; `openFileInEditor` puts the file in the pane on the files rail's side, never on the chat. The rails decide the side, so after Swap Side Panels (WS-211) a new session opens next to the sessions rail on its new side, with no second rule to keep in step. Reopening a session that sits in another centre pane, or in a rail, MOVES its tab back beside the sessions rail, so the pair reads sessions | editor again; a session already in that pane simply focuses, and further sessions stack there rather than splitting a third column. Centre-tiling fallback only when the layout offers no pane on that side.
**Tests:** W1 (`lib/__tests__/centre-placement.test.ts` — a session beside the sessions rail, a file beside the files rail, the side after a swap, and a reopened session moved back out of another pane or a rail; `session-placement.test.ts` — stacking and the fallback; `layout-serializer.test.ts` legacy-chat-tab prune), W2 (`new-session-chat-tab.spec.ts`, `windowing-regression` pills-gone).

### WS-221: Full-flow proof — a session's agent affects the filesystem (TUI + web)
**As a developer**, one deterministic journey proves the whole stack: new session → prompt → REAL `write_file` tool call → REAL permission approval in the console → file bytes on disk — through both the TUI and the web UI.
**Acceptance:** the fake Ollama server can script TOOL CALLS (`OllamaRule.toolCall` streams a native-Ollama `message.tool_calls` NDJSON round; the follow-up round is detected by a `role:"tool"` message in the request and answered with `replyAfterTool`). `write_file` is permission-gated by default (not in the safe-tool allowlist, default mode Ask): the TUI leg approves with `y`, the web leg clicks Allow on the real inline `PermissionInteraction` card. Files land in the session WORKSPACE — kiln for `cru chat` sessions, registered project root for web-created sessions (documented asymmetry).
**Tests:** W4 live `e2e/live/agent-fs.live.spec.ts` (hero project, serial): TUI leg via `tui_e2e_tests hero::agent_fs_leg_tui_write` (expectrl; asserts `notes/agent-tui.md` content on disk), web leg drives the shipped UI end-to-end and asserts `notes/agent-web.md`. Shared TUI-leg runner extracted to `e2e/live/tui-leg-runner.ts`. Run with `just web-test hero`.

### WS-222: Knowledge graph view (Obsidian-style)
**As a user**, I can open an interactive graph of my kiln — notes as nodes, wikilinks as edges — and explore it like Obsidian's graph view: smooth canvas, physics, hover neighborhoods, click-to-open.
**Acceptance:** `Open Graph View` (palette) opens a center `graph` tab rendering `GET /api/kiln/graph` (daemon `kiln.graph` over the resolved `note_links` v2 index — resolved edges join `notes[].path`; unresolved targets become ghost "phantom" nodes). Canvas force layout (d3-force) with wheel zoom-to-cursor, background pan, node drag (reheats the sim), hover dims non-neighbors, click opens the note in the editor, double-click/crosshair fits the view. Settings card (persisted to localStorage): search filter, tags/unresolved/orphans toggles, node-size/link-thickness display sliders, center/repel/link-force/link-distance physics sliders. Labels are hover-only — the hovered neighborhood's names fade in/out with an eased highlight transition; nodes are a uniform radius with a collide force enforcing minimum spacing. Idle settled graph costs no CPU (dirty-flag rAF).
**Tests:** W1 (`lib/graph/__tests__/build.test.ts` — filter semantics: phantom synthesis, tag dedup, orphan rules incl. hidden-phantom orphan, query pruning, edge dedup/self-link, position adoption). Rust: link_index graph tests + `kiln.graph` RPC corpus test. W2 GAP: canvas interaction journey (canvas has no DOM to assert on — needs pixel/screenshot assertions; promote if it regresses).

### WS-223: Review the agent's changes from a queue
**As a user**, I get a Changes panel listing everything the session changed since it started, grouped root → file → hunk, and I accept or reject each hunk from there.
**Acceptance:** `changes` registers in the RIGHT region beside Activity and Backlinks. The queue is the *composed* diff (`session_base` → worktree) from `review.list_hunks`, not a list of tool calls, so the action unit is the composed hunk. The badge counts what is owed and the unreviewed filter drains to "nothing left" rather than to an empty panel. Reject is `set_state('rejected')` — one call, and the daemon reverts on disk in it; there is no second verb and no deferred mark. The reject asks once first, and the receipt it leaves carries an **Undo** that calls `review.undo_reject` (WS-232). A hunk in flight refuses a second click. `external` hunks (the user's own edits, §5) render for context with NO reject affordance. Clicking a file opens it in the center buffer AND asks that buffer to scroll to the first hunk; expanding a hunk mounts a CodeMirror merge view (`HunkMergeView`, `@codemirror/merge` `unifiedMergeView`) over the hunk's before and after text, read-only, with unchanged lines collapsed, whose Accept and Reject controls call the daemon, never CodeMirror's own chunk action; an external hunk's merge view draws no Reject. Comments anchor to a **range**, not a hunk id (§6), and open comments list and resolve.
**Tests:** W1 (`ChangesPanel.test.tsx` — grouping/counts, external-no-reject, accept/reject payloads, the confirm and its decline, the receipt, in-flight guard, filter drain, empty and failed states, open-and-scroll, `an expanded hunk mounts the merge view with accept and reject controls`, `an external hunk's merge view offers accept and no reject`, comment range, resolve; `lib/__tests__/review-store.test.ts` — optimistic accept + re-list, shared subscription, `review_changed` debounce, event-driven refresh; `lib/__tests__/review-api.test.ts` — the seven REST calls and their error surfacing). W2 GAP: the accept-a-hunk-and-watch-it-leave journey — blocked on a deterministic fixture that produces a real composed diff; promote once the live tier can script a writing turn.

### WS-224: Review inline, in the real buffer
**As a user**, review happens in the file I am already reading — decorated lines and a per-hunk gutter chip — not in a separate side-by-side pane.
**Acceptance:** `FileViewerPanel` gains a CodeMirror decoration layer: every line of a composed hunk is toned (distinct tones for unreviewed, accepted, and `external`), and exactly ONE attribution chip sits on the hunk's first line. A pure deletion still marks the line it collapsed onto; a hunk past the end of the buffer is dropped rather than thrown on; hunks out of document order still build a valid decoration set. Hunks belonging to another file never reach this buffer. Clicking the chip scrolls the transcript to the `ToolCard` that wrote the hunk and highlights it; an `external` hunk has a chip but nothing to jump to, because no tool call owns it. A reveal request for THIS file scrolls the buffer and is consumed; a reveal for another file is left for the panel that owns it. The layer reinstalls if the host reconfigures the editor out from under it.
**Tests:** W1 (`editor/__tests__/review-decorations.test.ts` — tone ranges, single chip, chip target, layer install/reinstall idempotence; `FileViewerPanel.review.test.tsx` — per-file scoping, external marking, reveal consumption).

### WS-225: A blocked agent is loudly blocked, and cards say what they did
**As a user**, when the agent is held at the review gate I can see it, and every tool card tells me whether what it wrote still exists.
**Acceptance:** `SessionStatusChips` reports the **effective** review policy the daemon computes — an ACP session in `ask` mode is `post_turn`, never `pre_write`, because the daemon cannot enforce a pre-write gate on an external agent (open question 1). Since 2026-09-15 the policy takes NO chip: "gated" named a mechanism a reader could not decode, and the permission card already says when a write waits, so the policy rides the chip wrapper as `data-review-policy` for tests and plugins. A mode with no gate, and a daemon that predates the field, draw no chip, and nothing re-derives the policy from the mode id. While the daemon's `review_gate` event says `blocked`, a distinct "waiting on review" chip names the file it is waiting on and clears when the gate releases; the owed count ignores the user's own `external` edits. `ToolCard` stamps the daemon call id and publishes the tool name, offers one accept/reject pair per composed hunk still live, and marks the call **superseded** when nothing it wrote survives — at which point the accept/reject controls vanish, because the action unit is gone. A card says nothing review-shaped until the ledger has actually been read, and a call that proposed no edit is never called superseded.
**Tests:** W1 (`SessionStatusChips.review.test.tsx` — effective vs configured policy, ACP degradation, missing-field silence, blocked/released chip, count excludes external; `ToolCard.review.test.tsx` — call-id stamping, gutter-chip highlight, superseded detection incl. the not-yet-loaded and no-edit cases, accept/reject naming the composed hunk).

### WS-231: Zoom an image in its pane
**As a user**, opening an image gives me a real viewer: I zoom with the plain scroll wheel over the image, drag to pan once it overflows, and the pane's zoom never touches browser page zoom.
**Acceptance:** an image tab renders `ImageViewer` (keyed on the path, so a different image always opens at fit and zoom state stays pane-local). Toolbar: zoom out / live percentage / zoom in / fit-to-pane / 1:1, in the shared canvas-toolbar style, each button's title and aria-label naming its shortcut. Plain wheel over the image zooms about the cursor — no Ctrl, because ctrl+scroll triggers browser page zoom, which is the UX this pane exists to avoid — and the event is consumed so it never scrolls the pane. Fit is contain-downscale-only (a 16px icon is not blown up to pane size); explicit zoom clamps to 10%–1000% on a ×1.25 step / exponential wheel curve. Drag pans via native scroll offsets with pointer capture; double-click toggles fit ↔ 100% anchored at the click point; with the pane focused, `+`/`=` zoom in, `-` zooms out, `0` refits, `1` is actual size. Cursor-anchor and fit maths live DOM-free in `lib/image-viewer.ts` (the `canvas-viewport.ts` discipline).
**Tests:** W1 (`lib/__tests__/image-viewer.test.ts` — bounds, step/wheel curve, fit calculation, cursor-anchored scroll incl. flex-centering and clamping; `FileViewerPanel.test.tsx` — toolbar with named shortcuts, button/keyboard zoom, reset-on-new-image). GAP: no W2 journey — wheel/drag geometry needs a real layout, which jsdom cannot measure.

### WS-232: Decide a whole file or turn at once
**As a user**, I accept or reject every change in a file, or in the whole review, with one click, I take a reject back from the receipt it leaves, and I narrow the queue to what the current turn changed.
**Acceptance:** each file row with an unreviewed non-external hunk carries **Accept all** and **Reject all** (`accept-all-<path>`, `reject-all-<path>`); the panel header carries the review-wide pair (`changes-accept-all`, `changes-reject-all`), disabled while nothing is decidable or a bulk call is in flight. Accept all is one click and ONE `review.set_states` call with the hunk ids in composed order across every root. Reject all asks ONCE, naming the count and the file (or "every file in this review"), then sends one `set_states` call; a declined confirm sends nothing. The daemon applies the list in order and names each hunk it refused; the panel shows the refused hunks in ONE notification by path and line, never as a bare id when it can name them. Every reject, single or bulk, leaves an `info` notification that never auto-dismisses and carries **Undo**; Undo calls `review.undo_reject`, which pops the daemon's journaled stack — multi-level, one batch per user action, and still there after a daemon restart. An undo the daemon refused as stale names the hunks that moved on and leaves the batch on the stack; an empty stack says "Nothing left to undo." The transcript's `ToolCard` reject offers the same undo. A **Session / Turn** control (`changes-scope-session`, `changes-scope-turn`, `aria-pressed`) sets the session's listing scope in the store, shared by every consumer; under Turn the daemon lists only hunks a tool call of the current turn touched, the empty state reads "No changes in this turn yet.", a listing that answers for a scope the session has left is dropped, and a tool card is never marked superseded from a turn-scoped listing. The browser never holds `after_content` to write it back: every decision, undo and filter is a daemon call.
**Tests:** W1 (`ChangesPanel.test.tsx` `ChangesPanel — bulk decisions` — `reject all on a file asks once and then undoes as one`, `reject all is not sent when the confirm is declined`, `accept all on a file is one click and one call`, `the review-wide buttons decide every root in composed order`, `a file with nothing left to decide offers no bulk buttons`, `names the hunks the daemon refused in one notification`, `an undo the daemon refused says which hunks moved on`; `a single reject offers an undo that pops the daemon stack`, `the turn scope asks the daemon for the turn`; `ToolCard.review.test.tsx` — `a reject from the transcript offers the same undo as the panel`, `a turn-scoped listing never marks an earlier call superseded`; `lib/__tests__/review-store.test.ts` — the bulk reject as one ordered call, the optimistic bulk mark, the undo outcome, the scope and the dropped stale answer; `lib/__tests__/review-api.test.ts` — `setHunkStates`, `undoReject`, the turn scope). Rust (`crates/crucible-daemon/src/server/session/review/tests.rs` — `a_bulk_reject_applies_in_order_and_reports_the_hunk_it_no_longer_knows`, `a_bulk_accept_records_every_state_and_emits_one_review_changed`, `three_rejects_undo_in_reverse_order_one_at_a_time`, `a_bulk_reject_undoes_as_one_batch`, `an_undo_after_the_file_moved_on_is_refused_as_stale_and_keeps_the_batch`, `turn_scope_lists_only_hunks_the_current_turn_touched`, `an_unknown_scope_is_refused`; `review/tests/persistence.rs` — `a_reject_stack_survives_a_reload`, `a_journal_written_before_the_reject_stack_still_replays`; `crates/crucible-web/tests/route_contract_tests/review.rs` — `set_states_forwards_the_ids_in_order`, `undo_reject_reaches_the_daemon_with_the_path_session`, `an_unknown_scope_is_a_bad_request`). W2 GAP: the same fixture WS-223 waits on.

### WS-233: Find the pass whose notes are waiting
**As a user**, the reflection and consolidation passes that ran while I was away are one section in the sessions list, so the notes they wrote are somewhere I can find and dispose of.
**Acceptance:** a pass writes its notes in a session of its own (`session_type === 'plugin'`), so its writes are hunks in that session's review queue and the Changes panel disposes of them like any other session's edits. A pass has no workspace, so the project tree would file it under "No project" and the phone's project switcher could never reach it; both shells therefore carry their own section over one shared `reflectionSessions()` helper — desktop `reflections-section`, a `TreeSection` folded by default with its count on the header, between the tree and Archived; phone `compact-reflections`, cross-project above the project list, like the Inbox. The list below the section leaves plugin sessions out, so a pass is listed once, not twice. A section with nothing behind it takes no row on either shell. An archived pass is not listed, and the freshest is first. The daemon holds a session with an undecided queue out of the archive, which is what makes the section worth having.
**Tests:** W1 (`lib/__tests__/session-reflections.test.ts` — `keeps only the sessions a plugin started`, `leaves out an archived pass`, `puts the freshest first`; `SessionsPanel.test.tsx` — `lists a plugin session under its own section`, `offers no section when no plugin session is listed`; `mobile/__tests__/SessionsTab.test.tsx` — `lists a plugin session under Reflections, whatever project is chosen`, `offers no Reflections section when no plugin session is listed`). Rust (`crates/crucible-daemon/src/server/tests/persist_event.rs` — `the_archive_sweep_skips_a_session_with_unreviewed_hunks`; `crates/crucible-daemon/src/server/session/review/tests.rs` — `a_plugin_sessions_note_write_lands_in_its_own_review_ledger`). W2 GAP: no journey drives a real pass end to end; the pass itself is covered by the shipped Luau suite.

### WS-HERO: One session across web and terminal (cross-surface)
**As a user**, a session and its kiln notes are shared truth across the browser and the terminal — the daemon is the hypervisor, the consoles are stateless.
**Acceptance:** the web console resumes a session started in `cru chat` (turn 1 hydrates both sides); opening `from-tui.md` in the real editor shows the terminal's write; editing + saving changes the bytes on disk; a web-sent turn 2 is later visible from `cru chat --resume`; final history is 3 turns and the file carries both the terminal and browser edits.
**Tests:** the flagship live journey `e2e/live/hero.live.spec.ts` (serial), orchestrating TUI legs (`tests/tui_e2e_tests/hero.rs`) around the web console. Deterministic LLM turns via the fake Ollama server (`e2e/live/fake-ollama.ts`) + an injected `init.lua` (`hero-setup.ts`), whose arrival the setup proves by reading the daemon's effective config. Run with `just web-test hero`. This is the first live-tier story to exercise real agent turns (see infra #3 note).

---

## 3. Shell surfaces (Crucible Shell design)

The four-surface shell from the "Crucible Shell Options" design (turn 5): Home → Inbox → Session ↔ Edit, one connected app. All state stays daemon-side; the surfaces are views over tabs in the window manager.

### WS-301: Land on Home and pick up where I left off — NOT IMPLEMENTED
**As a user**, opening the web UI with an empty workspace lands me on Home: a greeting, kiln stats, resume-a-session, recent notes, and a needs-you strip when something waits on me.
**Status:** not implemented in the current shell. No `home` content type is registered (`register-panels.tsx`), so the auto-open-on-empty-layout behavior does not exist; the `HomePanel` component and its W1 suite are gone from the tree. Resume/recent/needs-you affordances live in the Navigator (sessions scope) and Inbox instead. Revive or retire this story before building on it.

### WS-302: See and answer everything waiting on me in the Inbox
**As a user**, the Inbox shows every pending interaction at the top — answerable in place without switching tabs — and every session with live status below.
**Acceptance:** pending permissions render the full interaction (args, scope, diff for writes) via the same `InteractionHandler` as the chat; responding POSTs `/api/interaction/respond`, clears the in-chat prompt (via `crucible:interaction-resolved`), drops the badge, and shows a resolved note; session rows show WAITING/STREAMING/state and open the session.
**Tests:** W1 (`InboxPanel.test.tsx` — all-clear, in-place permission with broadcast + badge drop; `attentionStore.test.ts` — polled-aggregate merge, local-shadows-remote). Rust: `agent_manager::tests::permissions::list_all_pending_permissions_aggregates_across_sessions`, `web::events` wire-parity tests.
**Resolved GAP (2026-07-11):** sessions without an open tab now surface via `session.pending_interactions` (daemon RPC) → `GET /api/interactions/pending` → attention-store polling (10s, visible-tab only); open-tab state shadows the poll. The same change fixed live interaction rendering: the SSE `interaction_requested` payload is now normalized server-side from the daemon wire shape (`{request_id, request:{kind, action:{type,…}}}`) to the flat shape the frontend renders — previously only the e2e mocks' hand-built flat frames ever rendered.

### WS-303: The header is the shell — Home, Edit ↔ Session, Inbox badge
**As a user**, the global header gives me the whole app: logo → Home, an Edit ↔ Session mode pill on the same content, a context line for where I am, and an Inbox button with an attention badge.
**Acceptance:** the active surface is derived from the focused center tab (chat → session, file → edit, home/inbox → themselves; neutral tabs don't change it); goSession focuses the active session's tab or starts a new session; goEdit focuses a file tab or opens the notes tree + empty editor; the badge equals the number of sessions with a pending interaction.
**Tests:** W1 (`shellStore.test.ts` surface mapping/sync, `attentionStore.test.ts` badge counting). W2 GAP: header click-through journey (Home → Inbox → Session ↔ Edit) — promote when it breaks once.

### WS-304: Go anywhere from the omnibox
**As a user**, Ctrl+P opens one omnibox that reaches every surface, note, session, and command, with `>` scoping to commands and `[[` scoping to notes.
**Acceptance:** GO items always present; kiln notes load on open ([[ quick switcher) and open in the editor; sessions resume; prefix routing scopes sections; filtering matches label/description/keywords; query resets on close.
**Tests:** W1 (`CommandPalette.test.tsx` — 18 tests incl. prefix routing and note loading).

### WS-305: The composer shows the session's context
**As a user**, the chat composer shows chips for the workspace the session acts in (⌁) and the kiln it knows (◆).
**Acceptance:** the new-session splash and the live session render the SAME composer core: `ComposerCard` (the capsule — textarea, microphone, send button, nothing else) with one `ChipRow` under it, built from a `ComposerChip[]` list of key/value entries rather than hand-placed pickers. The draft lists kiln, project, workspace target, runtime, agent and model; the live session lists model, mode, project as a static fact, kiln and the plugin status chips. A chip is never inside the capsule. An entry whose value is `''` reads as what it resolves to, marked `· default` (`Session folder · default`, `Internal agent · default`, the provider's model); the runtime and workspace-target chips take the project's real default from `CenterComposer`'s label functions. A static chip is a label with the full path in its title; the session's scope chips carry the real `workspace`/`kiln` VALUE alone. The review policy takes no chip: "gated" named a mechanism the reader did not have, so the policy rides the chip wrapper as `data-review-policy` for tests and plugins (WS-225). The prompt is a stadium (9999px) while it holds one line and drops to `--radius-composer` (18px) once the text needs a second line; the card states the count as `data-lines="one" | "many"`, and the border, the ember focus treatment and the docked flat top follow whichever radius is in force. The mode control is a circle the height of the chips that wears the current mode's icon and names the mode in its `aria-label`; its list opens under the pointer, on click and from the keyboard, closes on Escape, and draws a built-in mode as icon, name and description and a Lua-declared mode as its name alone.
**Tests:** W1 (`composer/__tests__/ChipRow.test.tsx` — a select, a static, a mode and a defaulted chip from one list; `ChatInput.test.tsx` and `CenterComposer.test.tsx` — both render the row below the capsule with every chip outside it, in list order, and the draft reads its defaults; `composer/__tests__/ComposerCard.test.tsx` — chips as data on the row below, `data-lines` flipping with a second line, the stadium and card radii in the CSS; `SessionScopeChips.test.tsx` — the scope hook through the shared row; `ChatModeControl.test.tsx` — the circle, hover/click/keyboard open, Escape, built-in rows with icons, Lua rows without; `SessionStatusChips.review.test.tsx` — the policy as data, not as a chip).
**Target chips and refusals (2026-09-15):** the draft composer's workspace and run-on chips name the project's ACTUAL default when nothing is chosen — "Container · default", "This PC · default", "master · default" — read off the row a provider flagged (`default` on the runtime axis, `current` on the workspace axis; the oci plugin flags its unnamed row exactly when an untouched session would be isolated), with "Project default" only while the provider's answer is loading. ACP sessions keep both chips: the daemon relocates an ACP agent into the session's sandbox (`acp_launch.rs`) and resolves a workspace target before create for every agent type. Every refused call on the session-creation path (create, the first folder listing, send, the model and mode lists) raises one notification carrying the daemon's sentence — the web unwraps the `RPC error: {…}` envelope into the body's `error.message`, and the store folds a sentence repeated within five seconds into one toast. Proof: `CenterComposer.test.tsx::names the runtime the provider flagged as the project default`, `::names this machine as the default when no provider claims the session`, `::says only that a default applies until the provider has answered`, `::names the current checkout as the workspace default`; `lib/__tests__/api-errors.test.ts`; `stores/__tests__/notificationStore.test.ts` (deduplication); `crates/crucible-web/src/error.rs`::the_body_carries_the_daemons_message_not_the_rpc_envelope.

**Project chip (2026-09-15):** the project chip is static. A session's workspace is fixed at creation — the daemon refuses `session.set_workspace` for every session — so the chip names the project the session acts in (or "Session folder") with the full path in its title, and opens no picker. An ephemeral session HAS a workspace — the daemon gives it `<session_scratch_dir>/<id>`, whose basename is the session id — and the chip reads "Session folder" for that too, because a 36-character identifier is the longest text on the row and names nothing the reader can use; the full path, id included, stays in the title. The kiln chip still attaches and detaches. Proof: `SessionScopeChips.test.tsx::the project chip names the workspace and is not a control`, `::an ephemeral session's own folder reads as one, with the path in the title`; `ChatInput.test.tsx::an ephemeral session reads "Session folder" too, keeping the path in the title`.

**The row folds, it never shrinks (2026-09-15):** the chip row is ONE line and no chip on it truncates or gives up width. Each `ComposerChip` states a `priority` — the draw order and the fold order, owned by the surface, not by `ChipRow`: the live session lists model 10, mode 20, project 30, kiln 40, status 90; the draft lists model 10, kiln 20, project 30, agent 40, workspace target 50, runtime 60. The row shows as many WHOLE chips as its measured width holds and folds the rest into a round `+N` button the height of the mode circle (`composer-chip-overflow`, `aria-label="N more"`), which opens on hover and on click, closes on Escape or an outside click, and lists each folded chip as a full row — a select chip keeps its own trigger and opens its options from there, a static chip shows its axis and its value, and every folded chip keeps its testid. A chip that measures zero wide (the status chips before a plugin or a review speaks) is on no row and is counted into no fold. Measurement reads the row's `clientWidth` and each chip's `offsetWidth`, remembering each width by key so a folded chip need not be redrawn to be re-measured; a chip the row has never drawn forces one pass that draws them all and reads a MICROTASK later, because Solid holds a signal written inside an effect until the update cycle ends and a same-turn read measures elements that are not in the document yet. `measure` is injectable so a test can state the row's width and each chip's. Proof: `composer/__tests__/ChipRow.test.tsx` — priority order, everything shown when it fits, the count when it does not, a chip given back when the button itself would not fit, folded testids inside the popover, a folded select still handing its pick back, hover and click and Escape, the click that follows a hover keeping the list open, a zero-width chip uncounted, no `truncate`/`max-w` on the row, and `ChipRow — measuring the real row` driving the default path off stubbed layout.
**GAP:** attach/detach (`+ kiln`, ✕) from the design is display-only for now — the daemon has no RPC to change a live session's kiln set; blocked on daemon support.

### WS-309: The composer completes commands, notes, files, and tags
**As a user**, typing `/` in any composer — the in-session chat input *or* the new-session launchpad — pops a list of the slash commands that actually exist, and `[[`, `@`, `#` complete notes, files, and tags from the session's kiln.
**Acceptance:** the list is served by `GET /api/commands` from the same constant `execute_command` dispatches on, so it can't advertise a command that doesn't run (or omit one that does — the hand-maintained frontend copy had already lost `/models`); each row shows the command and its description; ↑/↓ move, Enter/Tab accept, Esc closes; accepting a command that takes an argument leaves a trailing space, a nullary one doesn't; `[[` completes to a closed `[[Note]]`; a `/` inside a word (`src/lib`) is not a trigger; a failed command fetch closes the popup and retries on the next keystroke rather than caching the failure. The popup is portalled and flips above the input when there's no room below — it previously rendered `absolute top-full` from an input pinned to the bottom of an `overflow-hidden` column and was clipped away entirely.
**Tests:** W1 (`useAutocomplete.test.ts` — `/` opens, server-sourced items + descriptions, narrowing, insert-with-argument-space, path-separator inertness, fetch-failure retry, `[[` open/complete/closed-link inertness; `popup-placement.test.ts` — flip-up when bottom-anchored, downward with room, never exceeding available space, viewport clamping). Rust (`route_contract_tests::commands` — `/api/commands` shape and descriptions; `every_advertised_command_is_dispatchable` runs each declared command through the real handler so a declared-but-unhandled command fails the build).
**Note:** the two composers were byte-identical copies that had already diverged — only the in-session one wired completion up. Both now render one `ComposerCard`, which owns the textarea, the popup, and the voice input.

### WS-310: Complete `[[wikilinks]]` while writing a note
**As a user**, typing `[[` in a markdown buffer offers the kiln's notes, so I can link without leaving the editor to look up an exact title.
**Acceptance:** a `@codemirror/autocomplete` source fires on `[[` in markdown files only, listing notes from the buffer's kiln with the note path as the detail line (two notes can share a title across folders); accepting inserts the name and closes the link exactly once — a `]]` the user already typed is not duplicated — leaving the cursor past the link; the source is inert with no kiln, once the link is closed, and inside fenced code or frontmatter (a TOML `[[mcp.upstreams]]` header is code, not a knowledge link — the same rule the wikilink decorations follow).
**Tests:** W1 (`wikilink-completion.test.ts` — opens on `[[`, anchors past the brackets, prose/closed-link/code-context/no-kiln inertness, accept closes the link, no bracket duplication, path shown as detail).

### WS-311: Popouts stay inside the window
**As a user**, no picker or completion list ever renders half off-screen, wherever its trigger sits.
**Acceptance:** one `placePopup` helper positions every composer popout against the viewport — autocomplete lists and the model/kiln/project/agent/target chips; panels open downward by default and flip above only when below can't fit them *and* above is roomier (flipping on a tie makes lists jump as the transcript grows); height is capped to the space actually available so long lists scroll internally instead of overflowing; `left` is clamped so a trigger near the right edge (narrow window, docked panel) pulls its panel back inside with the same 8px margin it keeps from top and bottom. Chip panels are content-sized, so they re-measure on `requestAnimationFrame` — a microtask runs before layout and leaves the 220px first-pass estimate in place.
**Tests:** W1 (`popup-placement.test.ts` — flip-up/down, space capping, right-edge clamp for both anchor-width and content-width panels, panel wider than the window, gap handling in both directions).
**Live-verified 2026-07-28:** a chip popout ran 147px past the right edge of a 760px window; after the fix all five composer chips sit fully inside. The in-session chat popup opens upward with its bottom edge exactly on the input's top edge.

### WS-312: A reloaded chat pane is still connected to its session
**As a user**, reloading the page leaves my open chat exactly as usable as before — I can type immediately, and the model and scope chips still show the session's real settings.
**Acceptance:** a pane restored from the persisted layout announces its session through `statusBarStore.activeSessionId` (that is all `bootstrapSessionWithFallback` ever did — it synced the *status bar*, never `SessionContext`); `SessionContext` now adopts that id, hydrating `currentSession` without dispatching `crucible:open-session` (the user-initiated `selectSession` path opens a tab — re-dispatching would make a restored tab try to open itself). Adoption is skipped when the id is already current, drops a response whose id is no longer focused (fast tab switching), and leaves the previous session in place if the fetch fails rather than blanking the composer.
**Tests:** W1 (`SessionContext.test.tsx` — adopts a restored pane's session, follows a pane switch, no refetch when already current, survives a failed fetch).
**Live-verified 2026-07-28:** before, a reloaded pane left the composer disabled on "Select a session first…" with the model chip on "Select model" and no scope chips, while `GET /api/session/{id}` returned 200. After: enabled, `glm-5.2`, scope chips showing kiln `docs`, and `/` completion working in the restored pane.
### WS-313: The precognition badge survives a reload
**As a user**, reopening a session still shows which notes grounded each answer — the "Enriched with N notes" badge and its note list come back with the transcript.
**Acceptance:** `should_persist` (daemon `server/core.rs`) now writes `precognition_complete` to the session event log, so what context was injected is part of the turn's record rather than a live-only notification; history replay in `ChatContext.loadHistory` reattaches it to the user message that triggered it — the same target the live reducer picks — mapping the persisted `PrecognitionNoteInfo` (`title`/`score`) onto the UI's `name`/`relevance`, exactly as the SSE path normalises in Rust. The event contributes metadata, not a transcript bubble; a precognition event with no preceding user message (truncated or paginated history starting mid-turn) is ignored rather than attached to nothing.
**Why persisted, not recomputed:** re-running the search on load would report *today's* index against the badge's "this is what the answer was grounded in" claim — a fabricated record of an injection that never happened.
**Tests:** Rust (`server::tests::persist_event` — `precognition_complete` is on the persist allowlist; `test_persist_event_keeps_precognition_notes` pins the full payload, notes included, through the storage round-trip). W1 (`ChatContext.test.tsx` — badge restored from history with note names, orphan event ignored, multi-turn histories attach each to its own turn).
**Live-verified 2026-07-28:** with a `precognition_complete` record in a real session log, a reloaded transcript renders "Enriched with 3 notes" on the user message, expanding to the three notes with their relevance scores.

### WS-306: The status bar knows where I am
**As a user**, the status bar's left side shows the active surface (⌂/▤/✎/◆ + name) and the session's workspace · knows-kiln context.
**Acceptance:** surface indicator follows the header pill; context segment renders only when a workspace/kiln is known; static filler (Ready/UTF-8/TypeScript) is gone.
**Tests:** covered indirectly by W1 store tests; W3 baselines pin the rendered bar.

### WS-307: Sessions name themselves after their topic
**As a user**, once a session's first turn completes, it gets a short topic-based title everywhere (tab, Home resume, Inbox) — I never see a wall of "Session chat-202…" again.
**Acceptance:** the daemon generates the title with the session's own LLM provider on the first `message_complete` of an untitled session (truncation of the first user message as fallback) and broadcasts `title_changed`; the open tab, Home resume card, and Inbox lists update without a refresh; untitled sessions fall back to "Untitled · <date>" instead of colliding id slices.
**Tests:** daemon `agent_manager::title` unit tests (sanitize/truncate); web route `auto_title_delegates_to_daemon_generate_title`; reducer `title_changed` matrix + SSE parity tests.

### WS-308: Old sessions archive themselves out of my way
**As a user**, sessions idle for 3 days disappear from Home and the Inbox into a collapsed ARCHIVED section, where I can restore, delete one, or clear the whole history.
**Acceptance:** the daemon sweep archives idle sessions in storage (not just in-memory ones) after `auto_archive_hours` (default 72); Home resume and Inbox RECENT list only non-archived sessions sorted by last activity (Inbox capped at 30 with a count); the ARCHIVED section lazy-loads on expand and offers RESTORE, two-click DELETE, and a two-click CLEAR HISTORY bulk delete.
**Tests:** daemon `test_sweep_archives_stale_persisted_sessions_not_in_memory` pins the storage-sweep gap; sort/fallback helpers in `lib/session-display.ts`. GAP: no component test drives the ARCHIVED section UI yet (manual + live verification only).

### WS-314: Every session setting the daemon has is settable from the web
**As a user**, the settings panel exposes the whole session config surface — context budget, context window, autocompact threshold, max iterations, execution timeout, validation retries, context strategy, output validation and the system prompt — not just the five it used to, and it can read back the session's mode instead of assuming it.
**Acceptance:** an "Advanced Session" section loads every knob on mount and commits each one independently; numeric knobs commit on blur, with an empty field sending `null` (restore the daemon's default) rather than `0`; the enum knob round-trips its *string spelling* and keeps a value the dropdown does not know about, because the daemon owns the enum and answers 422 for a name it rejects; the system prompt debounces instead of firing a PUT per keystroke.
**Why the gate came first:** nine of fifteen knobs the daemon advertised in `METHODS` had no web route at all, and nothing failed — a knob missing from the axum Router is not a compile error in any crate. Gate **A2e** (`crucible-cli/tests/architecture_tests.rs`) now fails when the daemon advertises a knob the web cannot reach, *and* when a landed route is still listed as missing, so the ledger records work outstanding rather than work forgotten. It reads `SessionKnob::ALL` through the daemon's `rpc::rpc_set_method`, not a regex over `dispatch.rs`; the regex could not tell the method table from a `#[cfg(test)]` sample value, and it never saw `session.switch_model`, so the `model` knob went ungated on both front ends.
**The trap this closes:** `session.set_execution_timeout`'s wire field is `timeout_secs`. A request struct named after the knob compiles, passes review, returns 200, and drops the value. Route existence does not prove a value survives, so each knob is asserted in both directions instead.
**Tests:** Rust A2e (route existence for every knob `SessionKnob::ALL` holds, plus `agent_option`; ledger now empty) and `routes/session_config/tests.rs` (per-knob round-trip through the mock daemon — PUT's value read off the wire under the daemon's field name, GET's value under the web's response key, with per-knob-distinct values so a mis-wired route cannot pass by coincidence; plus `GET /api/session/{id}/mode`). W1 (`AdvancedSessionSettings.test.tsx` — load, per-knob commit, `null` on clear, non-nullable retries, enum spelling, unknown-enum passthrough, prompt debounce).

### WS-316: The daemon's own configuration, from the browser
**As a user**, the settings dialog shows Crucible's own configuration beside my
browser-local preferences, tells me which values my `init.lua` holds, and opens
the line that holds one.
**Acceptance:** a "Configuration" section renders the control tree
`GET /api/config` carries (`config.controls`: the same node vocabulary a
plugin's `cru.plugin.options{}` tree uses, so ONE renderer draws both), fed by
the daemon's effective config and its per-leaf provenance; a change writes
through `POST /api/config` (`config.save`) by config path. A leaf the daemon
reports as `pinned` renders LOCKED — the control is disabled, the note names
the file and the line, and a **jump-to-pin** button opens that line in the
editor and dismisses the dialog. The note never claims the user chose the value
everywhere: a pin may sit inside a test on the hostname, so it says the line may
be conditional on this host. A leaf that takes no control at all (the seven
location keys, the unbounded maps and lists, the free-form `plugins` subtree)
renders read-only WITH the daemon's reason. The four browser-local sections —
transcription, editor, appearance, terminal — stay in `localStorage` and nothing
migrates: fonts, terminal size, vim mode and the microphone are per-device.
**No credential crosses this door:** the effective config and the origin rows
both carry `web.api_key` and every `llm.providers.*.api_key`, and neither type
redacts on `Serialize`. The web layer redacts each answer as it arrives
(`services/daemon_config.rs`), so those leaves reach the browser as
`[redacted]` and the settings dialog shows a key is configured without showing
the key. The rule matches a leaf NAME, not a path, so a credential a future
struct adds is covered without anyone editing a list.
**Why the lock needs a route out:** Grafana, GNOME dconf and VS Code all refuse
a write against a higher layer, and all three say where to change the value. A
lock with no route out is a dead end, so the jump is part of the lock.
**Tests:** W1 (`settings/__tests__/AppConfigSettings.test.tsx` — a pinned leaf
renders disabled, names its file and line, jumps to it and closes the dialog; an
unpinned sibling saves by config path; a read-only leaf shows its reason).
Rust: `routes/config.rs` pass-through of the tree and the read-only reasons,
`rpc::dispatch` `config.controls` and the per-leaf `pinned` flag, and
`tests/config_secret_redaction_e2e.rs` (a real daemon boots an `init.lua` that
sets a provider key and the web key; neither string appears anywhere in the
`GET /api/config` body).

---

## 4. Shipped-feature stubs (stories owed under the "every feature adds a story" rule)

These features shipped without a WS entry. Each gets a stub here; flesh out acceptance criteria and promote tests when the story is next touched.

### WS-226: Canvas — spatial notes on an infinite surface
**As a user**, I open a `.canvas` document as a center tab and arrange cards (notes, files, links) on a pannable, zoomable surface.
**Acceptance (stub):** `canvas` registers as a center panel; documents load/save via `GET`/`PUT /api/canvas?path=…`; a canvas must live in a kiln and may only reference files inside that same kiln — the read path strips (quarantines) out-of-kiln references rather than reporting them.
**Tests:** W1 (`CanvasPanel.test.tsx`, `CanvasCard.test.tsx`, `CanvasLinkNode.test.tsx`, `lib/__tests__/canvas-doc.test.ts`, `canvas-viewport.test.ts`). GAP: no W2/W3 journey.

### WS-227: Browse agent skills
**As a user**, a Skills panel in the left region lists the skills available to the session's kiln, searchable, with per-skill detail.
**Acceptance (stub):** `skills` registers left; backed by `GET /api/skills` (kiln + optional scope), `GET /api/skills/search`, `GET /api/skills/{name}`.
**Tests:** W1 (`SkillsPanel.test.tsx`). GAP: no W2 journey.

### WS-228: Manage plugins from the browser
**As a user**, a Plugins panel lists installed plugins, lets me install/remove/reload them, run plugin commands, and edit plugin options.
**Acceptance (stub):** `plugins` registers left; seven endpoints: `GET`/`POST /api/plugins`, `DELETE /api/plugins/{name}`, `POST /api/plugins/{name}/reload`, `GET /api/plugins/publications`, `GET /api/plugins/options`, `POST /api/plugins/{name}/option`, `POST /api/plugins/command`.
The Plugins section of the settings dialog takes a git URL or the `user/repo`
shorthand and installs it through `POST /api/plugins`. ONE confirmation names
the URL before the clone, because a plugin runs with the user's own reach and
user installation is the security boundary this project relies on. After the
install the declared `cru.plugin.options{}` tree is re-read, so the plugin's
settings pane appears with no restart.
**Tests:** W1 (`PluginPanel.test.tsx`, `PluginSettings.test.tsx`), W2
(`e2e/plugin-install.spec.ts` — paste, confirm, the URL reaches the route
unchanged, the declared options render).

### WS-229: Search the kiln — text and semantic
**As a user**, the Navigator's search scope gives me one debounced box that searches notes as literal text (ripgrep) or by vector similarity, with hits that open in the editor.
**Acceptance (stub):** `SearchPanel` is embedded in the Navigator (no standalone tab); backed by `POST /api/search/grep`, `POST /api/search/semantic`, and `POST /api/search/vectors`.
**Tests:** W1 (`SearchPanel.test.tsx`). GAP: no W2 journey.

### WS-230: Clone a repo and start working in it
**As a user**, pasting a repo URL (https, git@, or `owner/repo` shorthand) in the new-session launchpad or the explorer's root dropdown clones it and registers it as a project.
**Acceptance (stub):** `POST /api/scm/clone` proxies the daemon's `scm.clone` (long timeout) into `[workspace] root_dir`; callers are `CenterComposer` and `RootDropdown`.
**Tests:** W1 (`CenterComposer.test.tsx`, `RootDropdown.test.tsx` clone paths). GAP: no live-tier clone.

### WS-315: Sign in when the server demands it
**As a user**, on a non-localhost deployment a 401 pops a sign-in modal; pasting the API key (from `cru web key`) signs me in without the key ever being readable by page JS.
**Acceptance (stub):** the api layer dispatches `crucible:auth-required` on 401 → `AuthTokenPrompt` modal → `POST /api/auth/login` exchanges the key for an HttpOnly session cookie → reload; Settings also accepts the key. `POST /api/auth/logout` revokes the presented session server-side, but **no UI affordance calls it yet** (GAP).
**Tests:** W1 (`AuthTokenPrompt.test.tsx`, `lib/__tests__/api-token.test.ts`); Rust (`routes/auth.rs` login/logout/expiry suite).

---

## 5. Mobile shell

Design: `docs/Meta/Architecture/Mobile Shell.md`. Product record: **Mobile Shell** (`P2`) in `docs/Meta/Product.md`.

### WS-317: Use Crucible on a phone
**As a user** on a phone-width screen, I get a shell made for it — an app bar over one content surface, with sessions and files as tabs in a left drawer and the open note's backlinks in a right drawer — not the desktop window manager squeezed. I browse one project's sessions at a time and switch projects from the drawer's header, reach every other panel from an overflow menu, edit notes without vim, and start a session in three steps.
**Acceptance:** below 768 px the page draws the compact shell, decided once at load; the compact shell never loads or saves the desktop layout (`/api/layout`); each drawer opens from its app-bar button and from an edge swipe, and closes on a scrim tap, Escape, a swipe or the hardware back button, one drawer at a time; a closed drawer is `inert`; an open one is a labelled modal dialog that traps Tab and returns focus; back never strips a hash deep link (`/#note=…`); app-bar buttons and drawer tabs are 44 px targets. The phone stamps `data-density="touch"` on the content pane and on the files drawer, which sets the thumb metrics for every tree it renders — 36 px rows, 14 px text, a 22 px icon slot — and 44 px for the Properties card header. The content surface with no tab open draws the shared `EmptyState`: "No note is open", with **Open a note** and **Start a session**. On Android with gesture navigation the edge swipe is the system's Back — the buttons are the guaranteed input.
**Tests:** W1 (`stores/__tests__/deviceStore.test.ts`, `stores/__tests__/tabStackStore.test.ts`, `lib/__tests__/shell-boot.test.ts`, `lib/__tests__/tab-host.test.ts`, `lib/__tests__/tab-host-routing.test.ts`, `components/mobile/__tests__/*` — shell choice, layout guard, content surface keyed on identity, drawer, back stack, swipe, drawer tabs, tab stack and its overview, and every shared opener landing in the phone's stack). W2 `e2e/stories/mobile-shell.story.spec.ts` — the one spec that runs at a phone's viewport (`devices['Pixel 7']`): the shell replaces the window manager, the drawer walk with its two tabs, back closing a drawer without leaving the page, the overflow menu opening a panel as a content tab, and the three-step session flow. W4 none: the compact shell adds no daemon call.

---

### WS-318: Keep a kiln on this device
**As a user**, I choose which kilns this device keeps offline and how much of each, read and edit their notes with no connection, and see what has not reached the daemon yet.
**Acceptance:** Settings → Offline lists every kiln with "Don't keep", "Notes only" and "Everything, with images", the size it costs (notes and attachments apart), and the count of unsent edits with Send now; choosing a mode fetches the kiln WHOLE and turning it off drops exactly what it kept. A note opens from the network when the daemon answers and from the kept copy when it does not. A note write the daemon never answered, whole or anchored, is queued and the app bar shows the count; a write the daemon ANSWERED and refused (403 on a read-only project, or a stale base) is raised or refused, never queued. The outbox holds ONE entry per note, and a later write to that note folds into it: a task tick folds into a queued whole write's text, two ticks compose into one edit set (a tick then an untick queues nothing), and a whole write replaces a queued tick. A queued write keeps the base it was edited from AND that base's text, so a whole write to a note the daemon changed meanwhile is retried with all three texts and merged; a merge that leaves regions stays in the outbox as a conflict, counted apart from the unsent edits, and nothing is written beside the note. A queued tick replays against the current note with no base; a tick the daemon cannot place is reported, and leaves no copy because it has no body. A write that lands from the outbox moves the open buffer's base to the answered hash. A whole write the drain turned into a conflict reaches the open buffer it was made from: the buffer goes dirty, keeps its text, and the notice points at where the conflict waits. The outbox refuses to drain into a daemon it did not come from. A cached attachment is drawn only through `<img>`.
**Tests:** W1 (`lib/offline/__tests__/*` — the memory store AND the shipped `idbStore` against `fake-indexeddb`, kept kilns, the mirror and its modes, the outbox's data-loss rules including a save that lands mid-drain, the fold table (`outbox.test.ts` `folds a second write to the same note`, `fold.test.ts`), `networkSink.write` sending the base and taking the answered hash, the anchored replay with no base, the landed rows (`drainOutbox` `names each write the daemon accepted`, `sync.test.ts` `onNoteLanded`), the conflicted rows (`sync.test.ts` `onNoteConflicted`, `EditorContext.test.tsx` `a drained conflict marks the open buffer and names the conflict`, `a buffer with another base is left alone`), the read that prefers queued writing and folds a queued edit into the mirror, the offline→reconnect round trip, image hydration; the base text every entry keeps and the merge on the drain (`sync.test.ts` `queues the base text the write was made from`, `sends the base text with a retried whole write`; `outbox.test.ts` `a stale whole write is retried with its base text and lands when the merge is clean`, `a stale anchored entry is retried as a whole write made from its base text`, `a folded write keeps the base text of the first queued write`, `an entry stored without a base text is reported as a conflict over the whole note, not dropped and not merged`), and the conflict that stays (`outbox.test.ts` `a stale write with regions stays queued as conflicted and writes no copy`, `a conflict is counted apart from what is still owed to the daemon`, `a conflicted entry is skipped by the next drain`, `a write arriving for a conflicted note replaces it with its own base`; `sync.test.ts` `tells the user where the conflict waits when no listener did`); `components/settings/__tests__/OfflineSettings.test.tsx`; `components/mobile/__tests__/OfflineBadge.test.tsx`).
**W4:** `e2e/live/conflict.live.spec.ts` now queues a committed save whose reply never reaches the browser, reloads the real IndexedDB outbox, introduces a competing write, and restarts the daemon during drain. The final retry clears the queue and preserves both writers' text, on desktop and at 390 px. The same-file conflict and refusal paths remain covered by `kiln-truth.live.spec.ts` and WS-322. **Remaining gap:** this cuts save replies, not the whole browser network; offline creation of a brand-new note and a fully disconnected cached-shell journey remain unproven.

---

### WS-319: Change a setting on a phone
**As a user on a phone**, I find a setting by reading one list and opening the category it is in, and I go back the way I came.
**Acceptance:** Settings opens on one scrollable list of every category, grouped, with no form shown and nothing to go back to. Tapping a category replaces the list with that category's settings and names it in the bar, beside a back control. Back returns to the list. A category that holds more categories — the daemon's configuration — shows each as a row with the count it holds, and opening one drills again, to any depth the tree has. The phone's own back button walks the levels; closing the dialog from any depth gives every history entry back, so the next back press belongs to the shell. Escape means up one level and closes only at the root. The desktop keeps its two-column dialog.
**Tests:** W1 (`settings/__tests__/settings-nav.test.ts` — the stack, the browser landing, and the entries a close returns; `settings/__tests__/SettingsModal.compact.test.tsx` — the root list, the drill, back, close and Escape). W2 (`mobile-shell.story.spec.ts` — the root list at a phone's viewport, a three-level drill through the config tree, the back button unwinding it, and the history a close gives back).

---

### WS-320: Tick a task without sending the whole note
**As a user**, I tick a checkbox in the reading view and only that line changes on disk.
**Acceptance:** A task box is interactive wherever the file can be written, and inert in a chat message or a hover card, where there is no file behind it. Ticking sends ONE anchored edit, not the whole note, so an edit made elsewhere in the same file between the read and the tap survives. The box that was tapped is the one that changes, even when several task lines are identical — the edit names which occurrence it meant. The tick is a note write through the same door as a save: it carries the buffer's base, and a PATCH with a base is refused when the note moved on, even when its anchor still applies; the outbox replay sends no base. The box flips immediately and the daemon's answer stands: a clean tick moves the buffer's base to the answered hash, and a refusal puts the box back and says why. When the daemon never answers, the tick is queued in the outbox and the box stays flipped; the app bar counts it. One queued write per note: a tick beside a queued whole write folds into that write's text, a second tick of the same line composes with the first (a tick then an untick queues nothing), and the daemon anchors every replayed edit on the note's current text with no base. A queued tick that lands while the note is open moves the buffer's base, and refreshes a clean buffer so the tick shows.
**Tests:** W1 (`lib/__tests__/task-toggle.test.ts` — the marker forms GFM allows, a flip that changes the box and nothing else, the occurrence count over identical lines, a line that is no longer a task; `editor/__tests__/MarkdownPreview.test.tsx` — inert without a writer, enabled with one, and the SOURCE line reported rather than the box's index; `editor/__tests__/EditorWithPreview.test.tsx` `task tick` — `ticks a task through editNote and keeps the flip when it is queued offline`, `reverts the flip and names the reason when the daemon refuses`, `a successful tick moves the base to the answered hash`; `lib/offline/__tests__/sync.test.ts` `editNote` and `replays a queued anchored entry against the current note with no base`; `outbox.test.ts` `folds a second write to the same note`; `contexts/__tests__/EditorContext.test.tsx` `a drained write refreshes a clean buffer's text`). W4 (`kiln-truth.live.spec.ts` — a tick against a real daemon leaves a concurrent edit to another paragraph byte-exact, an ambiguous anchor is refused, and naming the occurrence lands it).

---

### WS-321: Review on a phone
**As a user on a phone**, I open the Changes panel from the More menu and decide the agent's hunks with my thumb: every control is a target I can hit, and a whole file or the whole review is one tap and one confirm.
**Acceptance:** the compact shell offers `changes` in the More menu like every other panel that draws on a phone (WS-317). On the compact shell every review control — the per-hunk Accept, Reject and comment buttons, the per-file and review-wide pairs, the Session / Turn control, and the transcript chip's controls — is at least 44 px on both axes (`hit()` in `lib/touch.ts`, read from the device store once at load); the merge view's Accept and Reject controls are 44 px on every shell. The desktop shell keeps its dense controls. Nothing else differs: the same daemon calls, the same confirm, the same undo receipt (WS-232).
**Tests:** W1 (`ChangesPanel.test.tsx` — `on a compact shell every hunk control is at least 44 px`, `on the desktop shell the hunk controls keep their dense size`; `ToolCard.review.test.tsx` — `on a compact shell the transcript chip controls are at least 44 px`; `components/mobile/__tests__/MobileShell.test.tsx` — `offers the panels a phone can draw, and not the ones it cannot`). W2 GAP: no phone-viewport journey opens the Changes panel — it needs the same composed-diff fixture WS-223 waits on. W4 none: the phone adds no daemon call.

---

### WS-322: Resolve a note conflict on a phone
**As a user** whose note was changed by someone else while I was editing it, I keep both texts: the ordinary case merges itself, and when we both changed the same lines I choose, line group by line group, on the phone I am holding.
**Acceptance:** every note write carries the text it was made from beside its base hash, so a write the daemon refuses as stale is merged against the disk instead of being thrown away — a clean merge writes both writers' changes and the buffer takes what the route wrote. A merge that cannot settle a line group writes nothing: the write waits as a **conflict** in the outbox with the merged text and its **regions**, and nothing is written beside the note — there is no conflict copy. A conflict is counted apart from the unsent edits, because sending again can never settle it: the app-bar badge shows both counts, appears for a conflict even with an empty queue, and a tap opens the conflict instead of sending. The phone's More sheet offers `Conflicts (n)` only while one waits, and the Changes panel lists every conflict above the agent's hunks, with no session selected. Opening one draws the merged text in the editor with a block at each region: **Keep mine**, **Keep theirs**, **Keep both**, the words that differ marked on each side, a `N of M regions resolved` counter, and Previous / Next. Save is disabled until every region is settled, and leaving with one open asks first. Save writes the settled text against the hash the daemon answered with; a note that moved again in the meantime keeps the conflict open. Every control is a 44 px target.
**Tests:** W1 the merge layer (`components/editor/__tests__/conflict-regions.test.ts` — `a region widget maps when text above it is edited`, `a choice replaces the region and drops it from the open list`, `keeping both writes ours then theirs, with a line between them`, `draws one block widget per region, with the three choices`, `marks the words that differ on each side`); the view (`components/__tests__/ConflictView.test.tsx` — `keep mine and keep theirs rewrite the region and the counter`, `keep both keeps ours then theirs`, `save is disabled while a region is open and writes with the current hash`, `a whole-note region needs a choice before save is enabled`, `leaving with open regions asks`, `a note that moved again keeps the conflict open`); the panel (`components/__tests__/ConflictsPanel.test.tsx` — `registers "conflicts" as a tab either shell can open`, `lists what waits and opens one in the view`, `says so when nothing waits`, `draws the conflict another surface selected`); where it is met (`components/mobile/__tests__/OfflineBadge.test.tsx` — `counts conflicts apart from unsent edits`, `shows for a conflict even when the queue is empty`, `opens the conflict list instead of sending when one waits`; `components/mobile/__tests__/MobileShell.test.tsx` — `lists Conflicts when one waits`, `offers no Conflicts row when none waits`; `components/__tests__/ChangesPanel.test.tsx` — `lists a conflicted note above the hunks`, `lists a conflict with no session selected`, `opens the conflict a row names`); the desktop save (`contexts/__tests__/EditorContext.test.tsx` — `a stale desktop save with regions opens the conflict view and keeps the buffer dirty`, `a stale desktop save with a clean merge lands and moves the base`). Rust: the merge itself (`crates/crucible-core/src/note_merge.rs`::`a_cluster_both_sides_changed_takes_ours_and_reports_a_region`, `::region_lines_point_into_the_merged_text`, `::an_insert_by_theirs_at_the_start_of_our_hunk_merges_without_a_panic`) and the route (`crates/crucible-web/tests/route_contract_tests/kilns.rs`::`a_put_with_a_stale_base_and_base_text_merges_and_writes`, `::a_put_whose_merge_has_regions_writes_nothing_and_answers_them`, `::a_put_with_a_stale_base_and_no_base_text_is_refused`, `::a_patch_and_a_put_to_one_note_take_one_lock`). W4 (`e2e/live/conflict.live.spec.ts` — `WS-322: two writers on one line, settled in the conflict view`, run at a desktop viewport and at 390 px against a real daemon; `e2e/live/kiln-truth.live.spec.ts` — the two `WS-318:` merge legs, which prove the route's two answers).

---

### WS-323: An open note hears the watcher
**As a user** with a note open, I am told when something else changes it on disk, and I never silently lose either text.
**Acceptance:** while a file is open the editor listens to the kiln watcher (`GET /api/fs/events`) and stops listening when the last buffer closes. A **clean** buffer whose note changed re-reads it quietly and takes the new text and base — unless it went dirty or its base moved while the read was out, in which case the answer is dropped. A **dirty** buffer is never overwritten and never re-read behind the user: it shows a banner, `This note changed on disk — your unsaved edits are still here`, with **Reload** and **Merge**. Reload takes the disk's text and asks first, because the buffer's bytes exist nowhere else. Merge saves, which carries the buffer's base text, so the ordinary case merges and lands, and a merge that leaves regions opens the conflict view (WS-322). A re-read that fails raises the banner rather than leaving the buffer quietly wrong. A move is heard at both ends; a delete is left alone. A landed save clears the banner; a refusal or a conflict leaves it, because the disk is still ahead.
**Tests:** W1 (`contexts/__tests__/EditorContext.test.tsx` — `listens to the watcher only while a file is open`, `a clean buffer takes the disk text and the new base`, `a dirty buffer keeps its text and says the disk moved`, `a note moved out from under a dirty buffer says the disk moved`, `a clean buffer whose re-read fails says the disk moved`, `a path no buffer holds is left alone`, `reload takes the disk text and the new base`, `reload asks before it discards unsaved edits`, `merge saves with the buffer's base text and clears the flag`; `components/__tests__/FileViewerPanel.test.tsx` — `offers reload and merge when the disk moved under a dirty buffer`, `draws no banner while the buffer and the disk agree`). W4 (`e2e/live/kiln-truth.live.spec.ts` — `WS-323: an open note shows the banner when a second writer PUTs it`).

---

## Infra requirements these stories impose (status)

1. **vitest gates CI** — DONE: `just ci` runs `web-test unit`; the GitHub `test-web` job runs `bunx vitest run` (617 tests).
2. **Story specs run with video + trace ON** and step screenshots — DONE: the `stories` Playwright project (`e2e/stories/**`) with `createStory().step()`; the existing default project keeps its cheap settings.
3. **W4 harness** — DONE: `playwright.live.config.ts` + `e2e/live/global-setup.ts` boot `cru web` on an isolated socket against a `TempDir` kiln; teardown stops the daemon and kills the tree. Gated on a `cru` binary (`CRU_BIN`); skips cleanly otherwise. NOTE: the web session route hardcodes the internal agent, so `mock-acp-agent` is unreachable — but the hero tier (`playwright.hero.config.ts`) now makes real internal-agent turns deterministic by pointing the daemon's Ollama provider at a fake Ollama server (`e2e/live/fake-ollama.ts`) via an injected `init.lua`. The base live tier still covers the no-LLM kiln-truth path.
4. **Visual baselines** committed under `e2e/__screenshots__/` — DONE: markdown editor + chat mid-stream/complete, each eye-verified before commit.

## See Also
- [[TUI User Stories]] — terminal counterpart
- [[Meta/Product]] — Web & Desktop feature inventory
