// Graduation invariant tests removed — graduation logic moved to the app layer (crucible-cli).
// The oil crate no longer has scrollback nodes, GraduationState, or ElementKind.
// Property-based graduation tests should be recreated at the app layer if needed.
