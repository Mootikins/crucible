//! Theme configuration for Crucible Desktop
//!
//! Uses gpui-component's theming system.
//! Theme initialization is handled by gpui_component::init() in app.rs.

// Theme is initialized via gpui_component::init(cx) which calls theme::init(cx)
// This module can be extended for custom theme configuration if needed.
