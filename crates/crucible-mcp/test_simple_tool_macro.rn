/// Simple test for #[tool] macro
#[tool(desc: "Create a test note")]
pub async fn create_test_note(title: string, content: string) {
    #{
        success: true,
        title: title,
        content: content,
        message: `Created note: ${title}`
    }
}

#[tool(desc: "Search for something")]
pub async fn search(query: string, limit?: number) {
    #{
        results: ["result1", "result2"],
        query: query,
        limit: limit ?? 10
    }
}