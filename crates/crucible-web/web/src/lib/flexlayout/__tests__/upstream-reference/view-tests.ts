/**
 * FlexLayout Upstream View Tests Reference
 * 
 * This file contains ALL 71 test names extracted from the upstream FlexLayout
 * repository (caplin/FlexLayout) tests-playwright/view.spec.ts
 * 
 * These are used as a reference for exact E2E test reproduction in Task 11.
 * Each test name must be reproduced exactly with matching interactions.
 */

// drag tests two tabs > tab to tab center
// drag tests two tabs > tab to tab top
// drag tests two tabs > tab to tab bottom
// drag tests two tabs > tab to tab left
// drag tests two tabs > tab to tab right
// drag tests two tabs > tab to edge

// three tabs > tab to tabset
// three tabs > tab to tab center
// three tabs > tab to tab top
// three tabs > tab to tab bottom
// three tabs > tab to tab left
// three tabs > tab to tab right
// three tabs > tab to edge top
// three tabs > tab to edge left
// three tabs > tab to edge bottom
// three tabs > tab to edge right
// three tabs > row to column
// three tabs > row to single tabset
// three tabs > move tab in tabstrip
// three tabs > move tabstrip

// borders > border top to tab
// borders > border bottom to tab
// borders > border left to tab
// borders > border right to tab
// borders > tab to border top
// borders > tab to border bottom
// borders > tab to border left
// borders > tab to border right
// borders > tab to open border top
// borders > tab to open border bottom
// borders > tab to open border left
// borders > tab to open border right
// borders > tab to open border top center
// borders > tab to open border bottom center
// borders > tab to open border left center
// borders > tab to open border right center
// borders > move tab in border top
// borders > move tab in border bottom
// borders > move tab in border left
// borders > move tab in border right

// Overflow menu > show menu

// Add methods > drag to tabset
// Add methods > drag to border
// Add methods > add to active tabset

// Delete methods > delete tab
// Delete methods > delete all tabs

// Splitters > vsplitter tests > vsplitter
// Splitters > vsplitter tests > vsplitter to edge
// Splitters > vsplitter tests > vsplitter to edge left
// Splitters > horizontal > resizes with hsplitter
// Splitters > horizontal > moves hsplitter to bottom edge and back
// Splitters > horizontal > moves hsplitter to top edge and back

// Maximize methods > maximize tabset using max button
// Maximize methods > maximize tabset using double click

// Others > rename tab
// Others > rename tab cancelled with esc
// Others > click on tab contents causes tabset activate
// Others > click on tab button causes tabset activate
// Others > click on tabstrip causes tabset activate

// tab can have icon

// Extended App > onRenderTab
// Extended App > onRenderTab in border
// Extended App > onRenderTabSet
// Extended App > onRenderTabSet sticky buttons
// Extended App > onRenderTabSet for border

// Extended layout2 > check tabset min size
// Extended layout2 > check border top min size
// Extended layout2 > check border bottom min size
// Extended layout2 > check border left min size
// Extended layout2 > check border right min size
// Extended layout2 > tabset close
// Extended layout2 > borders autohide top
// Extended layout2 > borders autohide left

export const VIEW_TEST_COUNT = 71;
