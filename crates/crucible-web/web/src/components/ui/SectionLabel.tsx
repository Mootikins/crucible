/**
 * The mono uppercase micro-label used for card/section headings (Home cards,
 * Inbox sections). One class string so the three call sites can't drift —
 * compose extra spacing/interaction classes at the call site.
 */
export const SECTION_LABEL_CLASS =
  'font-mono font-semibold text-[10px] tracking-[0.08em] text-muted-dark';
