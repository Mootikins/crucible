# Routes the axum router serves that the OpenAPI document does not describe.
#
# `every_route_the_router_serves_is_in_the_document` allows exactly these and
# nothing else, so a NEW undescribed route fails at once.
# `the_openapi_baseline_only_shrinks` fails when a line here is no longer
# missing, so the file can only lose lines.
#
# Tasks A5 to A10 each convert one route group. Each deletes its own lines in
# the same commit that converts the group. A10 deletes this file, and the gate
# then demands that nothing is missing.
#
# One `METHOD /path` per line, sorted.
GET /api/agents
GET /api/commands
GET /api/config
GET /api/interactions/pending
GET /api/mcp/status
GET /api/skills
GET /api/skills/search
GET /api/skills/{name}
GET /api/terminal/ws
POST /api/chat/send
POST /api/config
POST /api/interaction/respond
POST /api/session/{id}/command
POST /api/webhook/{name}
