# Rust
target/
**/*.rs.bk

# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
package-lock.json
pnpm-lock.yaml

# Build outputs
dist/
build/
*.tgz
# Vitest / v8 coverage reports (web frontend)
coverage/

# SvelteKit
.svelte-kit/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Environment
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Database
*.db
*.sqlite
*.sqlite3
*.db-shm
*.db-wal
.crucible/

# Demo configs (contain local endpoint/paths — copy from .example)
assets/demo-config.toml
assets/demo-acp-config.toml

# Tauri
src-tauri/target/

.claude/
.crush/
.cursor/

# AI agent symlinks
CLAUDE.md
crates/crucible-llm/.fastembed_cache/

# OpenCode IDE
.opencode/
.aider*

# Temporary directories
# Anchored: this is the git-worktree convention at the REPO ROOT
# (tree/feat/<name>). Unanchored, it matches any directory named tree/
# anywhere — which silently swallowed web source for two days.
/tree/
# Some developers also carry an unanchored `tree/` in their GLOBAL gitignore,
# which this repo's rules override. A file cannot be re-included once its
# parent directory is excluded, so the directory itself is un-ignored here.
!crates/crucible-web/web/src/components/tree/
.worktrees/
.cargo/*
!.cargo/config.toml
WORKTREE.md

# Session artifacts (from session architecture tests)
**/Sessions/

# Local thoughts/plans (not committed)
thoughts/

# Plans and research are working notes, not repository content. They date
# instantly, they cite line numbers that move, and — because `docs/` is a
# validated kiln — a draft left here fails `just lint docs` for everyone on
# broken wikilinks and stale code refs in a file no commit contains.
# `/2_create_plan` and `/1_research_codebase` already write to `thoughts/`.
docs/Meta/Plans/
crates/crucible-cli/proptest-regressions/
.sisyphus/

# Debug artifacts
*.png
*.txt
# ...but generated test-tier manifests are required by architecture tests on CI
!assets/test-tiers/*.txt
.ruff_cache/
.playwright-mcp/
reproduce.jsonl
reproduce.cast

# Wayfinder maps / scratch tickets (not committed)
.scratch/

# Local test config referenced from .env.local (see .env.local.example)
.crucible-test-config.toml

# graphify knowledge-graph output (regenerable, ~40MB)
graphify-out/

# Personal launch notes, kept out of the repo on purpose.
LAUNCH-CHECKLIST.md
