-- The fragment: what discovery reads about `auto-title` before any plugin code runs.
return {
  name = "auto-title",
  version = "0.1.0",
  description = "Name a session after what it is about",
  author = "Crucible Team",
  license = "MIT",
}
