-- The fragment: what discovery reads about `session-board` before any plugin code runs.
return {
  name = "session-board",
  version = "1.0.0",
  description = "Every live session as a surface, in the TUI and the browser",
  author = "Crucible Contributors",
  license = "MIT",
}
