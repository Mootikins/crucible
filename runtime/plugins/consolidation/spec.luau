-- The fragment: what discovery reads about `consolidation` before any plugin code runs.
return {
  name = "consolidation",
  version = "0.1.0",
  description = "Periodic pass over recent sessions that proposes pattern notes",
  author = "Crucible Team",
  license = "MIT",
}
