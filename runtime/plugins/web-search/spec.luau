-- The fragment: what discovery reads about `web-search` before any plugin code runs.
return {
  name = "web-search",
  version = "0.1.0",
  description = "Web search over a configurable provider chain",
  author = "Crucible Team",
  license = "MIT",
}
