-- The fragment: what discovery reads about `worktree` before any plugin code runs.
return {
  name = "worktree",
  version = "0.1.0",
  description = "Run a session against a git worktree — the workspace axis",
  author = "Crucible Team",
  license = "MIT",
}
