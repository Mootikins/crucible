-- The fragment: what discovery reads about `review` before any plugin code runs.
return {
  name = "review",
  version = "0.1.0",
  description = "Review the attributed diff of a session — including one you delegated to",
  license = "MIT",
}
