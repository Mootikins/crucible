# Files currently exceeding the 1500-line ceiling enforced by
# scripts/check-file-sizes.sh. Each entry is a decomposition target.
#
# Rules:
#   - New files over the ceiling are rejected by CI.
#   - Remove an entry once the file is decomposed below the limit.
#   - Adding a new entry requires a comment saying why the file cannot
#     be split yet, and what would unblock it.

# Blocked on the daemon-owns-setup refactor.
crates/crucible-daemon/src/provider/genai_handle.rs

# dispatch.rs is the canonical RPC method registry: METHODS const +
# match arms for every method + per-method handler wrappers. Each new
# RPC adds 20-40 lines structurally. Slated for a registry-table
# decomposition (one match arm per category) once the table shape
# stabilises.
crates/crucible-daemon/src/rpc/dispatch.rs
